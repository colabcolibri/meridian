---
id: v2
title: Visual Studio Bridge
status: active
outcome: "Developer edits docs/us and docs/epics in VS Code with sync and integrated board."
---

# v2 — Visual Studio Bridge

## Objective

Editor extension for real Markdown writes in the workspace folder, board generation, and Meridian operation without relying only on the browser.

## Done criteria

VSCode user creates a US referencing existing epic and version, saves to disk, and board.json regenerates without a fragile manual step.

## Included in this version

- VS Code extension in `app-visual-studio/` (EPIC-05).
- Script `generate_board.py` callable from extension, CI, and agents.
- Commands: sync board, validate project, new user story.
- Writes to `docs/us/`; templates for `docs/epics/` and `docs/versions/`.
- Monitor read-only stale-board warning (EPIC-04).
- Board documentation UX: validate `## Technical implementation`, Doc badge on cards, lean columns (EPIC-03/04 — US-0073–0075, sprint v2-S5).
- Kit docs and CI for the extension.

## Explicitly out

- Web monitor as markdown author (remains reader).
- Drag-and-drop kanban status changes.
- Backend, auth, or multi-user.
- Tauri / Python validation without dev server (deferred).
- Full epic/version wizards (only templates in v2; wizards → later).

## Go-live checklist

### Product

- [ ] `app-visual-studio/` loads in Extension Development Host (US-0041).
- [ ] Workspace with `.agent/` + `docs/` detected (US-0042).
- [ ] `Meridian: Sync Board` regenerates `board.json` (US-0045).
- [ ] `Meridian: Validate Project` runs validator (US-0046).
- [ ] `Meridian: New User Story` creates valid US file (US-0047–0049).
- [ ] Epic/version templates available (US-0050).
- [ ] Monitor warns when board is stale (US-0051).
- [ ] Board shows Doc / Sem doc on cards and hides empty columns (US-0073–0075 — Should for v2 go-live).

### Quality

- [ ] Extension CI in GitHub Actions (US-0052).
- [ ] `python3 .agent/scripts/validate_meridian.py app-desktop` passes.
- [ ] `generate_board.py` output matches skill `generate-board-json` schema.

### Documentation

- [ ] `05_architecture.md`, `01_tech_stack.md`, `02_security.md`, `08_environments.md` cover extension layer (US-0043).
- [ ] Kit reference for extension workflow (US-0052).
- [ ] EPIC-05 marked `complete` (US-0053).

## Sprints

- `v2-S1` — Extension foundation (US-0041–0043)
- `v2-S2` — Board sync and validation (US-0044–0046)
- `v2-S3` — Create user stories on disk (US-0047–0049)
- `v2-S4` — Templates, monitor polish, go-live (US-0050–0053)
- `v2-S5` — Board documentation UX (US-0073–0075)
