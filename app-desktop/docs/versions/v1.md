---
id: v1
title: Folder Monitor MVP
status: complete
outcome: "Usuário abre docs/, as 3 abas refletem .md reais sem mock; validações visíveis."
---

# v1 — Folder Monitor MVP

## Objetivo

Gerenciar o projeto **pelos mesmos arquivos** que agentes editam (`docs/`, `docs/us/`, `board.json`).

## Critério de Done

Usuário abre a pasta `docs/` do projeto (ex.: `app-desktop/docs/`); as 3 abas refletem os `.md` sem mock nem `data.ts` como fonte.

## Incluído nesta versão

- File System Access para escolher pasta `docs/`.
- Parser de frontmatter para docs de fase, épicos, versões e `docs/us/`.
- Validações alinhadas ao protocolo e `validate_meridian.py`.
- Guia **Comece aqui** e UX do monitor (EPIC-06).

## Explicitamente fora

- Edição de US/docs pelo browser.
- Tasks/Bug/Spike como artefatos separados (permanecem dentro da US ou no chat).

## Checklist go-live

### Produto

- [x] Abrir pasta `docs/` via File System Access (US-0009).
- [x] Três abas com dados reais da pasta aberta (US-0011, US-0012, US-0013).
- [x] Ler cada doc 00–11 inline (US-0017).
- [x] Validações de protocolo na UI (US-0014); mock removido (US-0016).

### Qualidade

- [x] `pnpm test` e `pnpm build` passam.
- [x] `validate_meridian.py app-desktop` passa (dev: botão Validar pasta).

### Documentação

- [x] `07_architecture.md` approved (pasta monitorada = `docs/`).
- [x] `board.json` derivado de `docs/us/`.

## Sprints

- `v1-S1` — Leitura real da pasta
- `v1-S2` — Experiência do monitor (UX)
- `v1-S3` — Polish Entregas + Quadro (US-0023–0027)
