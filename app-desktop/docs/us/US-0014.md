---
id: US-0014
title: Protocol validations in the UI
epic: EPIC-03
version: v1
status: ✅
moscow: Must
depends_on:
  - US-0011
  - US-0013
done_when: App highlights doc and US inconsistencies per P0 MERIDIAN.md rules in opened folder.
tests: required
tests_status: done
---

# US-0014 — Protocol validations in the UI

**As** Process Manager,
**I want** visual alerts when the folder violates the Meridian protocol,
**so that** I fix documentation before agents implement incorrectly.

## Acceptance

- [x] Dependent doc `approved` with predecessor not `approved` → alert in Initial setup.
- [x] US `🔶` without `Missing:` line in acceptance → alert in Kanban.
- [x] US `✅` without recorded evidence (empty Tests section) → visible warning.
- [x] Panel or **N issues** badge in the shell when folder is open.
- [x] Rules implemented in pure functions (`protocol-validators.ts`, `story-body.ts`).

## Context & constraints

### Architecture refs

- docs/05_architecture.md — Phase documents and governance flow
- docs/11_decisions.md — Decision log structure
- docs/epics/EPIC-03.md — Epic scope and out-of-scope

### API / DB impact

- Documentation-only — no API or database changes.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Implementation hints (preliminary)

- Delivered scope recorded under `## Technical implementation`.
- Dependent doc `approved` with predecessor not `approved` → alert in Initial setup.
- US `🔶` without `Missing:` line in acceptance → alert in Kanban.

## Technical implementation

### Frontend

- `collectProtocolIssues`, `MonitorIssuesBanner`, badge in `FolderToolbar`.
- Inline alerts in `SetupMonitorView` and `KanbanView`.

## Tests

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

### Planned

- [x] **automated** — `pnpm test` — `protocol-validators.test.ts`.
- [x] **build** — `pnpm build` passed.
- [x] **dogfooding** — `08_environments` approved with `07` draft generates alert.

### Executed

- `pnpm test` — `protocol-validators.test.ts`.
- `pnpm build` passed.
- Dogfooding: `08_environments` approved with `07` draft generates alert.

## Out of scope for this story

- Run Python script (US-0015).
- Auto-fix files.

## Notes

- EPIC-03 delivered in the UI for reading; Python script remains US-0015.
