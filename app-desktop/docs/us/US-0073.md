---
id: US-0073
title: Validate technical implementation section in monitor
epic: EPIC-03
version: v2
status: ✅
moscow: Must
depends_on:
  - US-0014
done_when: Monitor flags US with status ✅ or 🔶 when ## Technical implementation is missing or still placeholder, using pure functions in story-body.ts.
tests: required
tests_status: done
---

# US-0073 — Validate technical implementation section in monitor

**As** Process Manager,
**I want** protocol alerts when a US is marked done without a real implementation record,
**so that** I catch documentation gaps before trusting the board.

## Acceptance

- [x] `getTechnicalImplementationStatus(body)` in `story-body.ts` returns `documented`, `placeholder`, or `missing` (section absent or only `_(fill in...)_`).
- [x] `documented` requires real paths in `### Files` or filled layer sections (not `_n/a_` alone on every layer with empty Files).
- [x] `validateStoryBody` or `collectStoryProtocolIssues` emits **error** when `status: ✅` and status is not `documented`.
- [x] Emits **warning** when `status: 🔶` and status is `missing` or `placeholder` (optional partial doc).
- [x] No alert for `status: ❌` (documentation not expected yet).
- [x] Issues appear in `MonitorIssuesBanner` with file `us/US-XXXX.md` and target id.
- [x] Automated tests cover documented, placeholder, missing, and ✅ gate cases.

## Context & constraints

### Why this story

This story delivered **Validate technical implementation section in monitor**: `getTechnicalImplementationStatus(body)` in `story-body.ts` returns `documented`, `placeholder`, or `missing` (section absent or only `_(fill in...)_`).. It existed so that I catch documentation gaps before trusting the board. The record of what shipped lives under `## Technical implementation`.

### Where it fits

Release **v2** (`EPIC-03` in frontmatter — not duplicated here). Depends on US-0014 — read those US files for what must already work. Stops before: Kanban card badge UI (US-0074).

### Approach

- Primary files: `src/domain/meridian/story-body.ts`, `src/domain/meridian/protocol-validators.ts`, `src/domain/meridian/story-body.test.ts`.
- `getTechnicalImplementationStatus(body)` in `story-body.ts` returns `documented`, `placeholder`, or `missing` (section absent or only `_(fill in...)_`).
- `documented` requires real paths in `### Files` or filled layer sections (not `_n/a_` alone on every layer with empty Files).
- Touch `src/domain/meridian/protocol-validators.test.ts` as part of delivering this slice — keep changes aligned with acceptance.

### Architecture refs

- docs/05_architecture.md — Phase documents and governance flow
- docs/11_decisions.md — Decision log structure
- docs/epics/EPIC-03.md — Epic scope and out-of-scope

### API / DB impact

- Documentation-only — no API or database changes.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

## Technical implementation

### Files

- `src/domain/meridian/story-body.ts` — `getTechnicalImplementationStatus`, validation hooks in `validateStoryBody`
- `src/domain/meridian/protocol-validators.ts` — `severityForStoryMessage` (warning for 🔶, error for ✅)
- `src/domain/meridian/story-body.test.ts` — unit tests for status detection and gates
- `src/domain/meridian/protocol-validators.test.ts` — banner severity tests
- `src/domain/meridian/parser.test.ts` — EPIC-05 status expectation aligned to `active`

### Backend

- _n/a_

### Frontend

- Pure functions; issues flow through existing `collectStoryProtocolIssues` → `MonitorIssuesBanner` and kanban card borders.

### Scripts / Docs

- _n/a_

## Tests

### Planned

- [x] **automated** — `pnpm test` — `story-body.test.ts` and `protocol-validators.test.ts` for implementation status rules.
- [x] **manual** — open folder with a ✅ US without `## Technical implementation` → error in issues banner.

### Executed

- `pnpm test` — 41 passed (story-body + protocol-validators coverage for documented/placeholder/missing and severity).
- Manual: dogfood folder surfaces errors for ✅ US missing implementation section in issues banner.

## Out of scope for this story

- Kanban card badge UI (US-0074).
- Python `validate_meridian.py` parity (future; browser uses TypeScript validators).
- Auto-fill implementation section (extension `/complete-us` — US-0062).

## Notes

- Extends US-0014; reactivates EPIC-03 for v2 read-path validation.
- Uses existing `storyBodies` map from project loader — no new disk reads.
