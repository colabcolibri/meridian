---
id: US-0028
title: Board with consistent visual tokens
epic: EPIC-06
version: v1
status: ✅
moscow: Must
depends_on:
done_when: Kanban and issue banner use bg-card/border-border; filters in single panel; no loose zinc-*.
tests: required
tests_status: done
ready: true
---

# US-0028 — Board with consistent visual tokens

**As** Process Manager,
**I want** a board visually aligned with the rest of the app,
**so that** I read cards and filters without color inconsistency.

## Intent

### Acceptance

- [x] Cards and columns use shadcn tokens (`bg-card`, `border-border`, `text-foreground`).
- [x] Filters grouped in panel (`monitorPanelClass`).
- [x] Filter chips reuse `filterChipClass`.
- [x] Issues banner uses the same panel and color tokens.
- [x] Evidence: `KanbanView.tsx`, `MonitorIssuesBanner.tsx`, `monitor-ui.tsx`.

### Why

This story delivered **Board with consistent visual tokens**: Cards and columns use shadcn tokens (`bg-card`, `border-border`, `text-foreground`).. It existed so that I read cards and filters without color inconsistency. The record of what shipped lives under `## Technical implementation`.

### Where

Release **v1** (`EPIC-06` in frontmatter — not duplicated here). Depends on US-0023 — read those US files for what must already work. Stops before: Boundaries defined in `docs/epics/EPIC-06.md` and parent version `v1`.

## Plan

### Approach

- Primary files: `src/features/monitor/components/KanbanView.tsx`, `src/features/monitor/components/MonitorIssuesBanner.tsx`, `src/features/monitor/monitor-ui.tsx`.
- Cards and columns use shadcn tokens (`bg-card`, `border-border`, `text-foreground`).
- Filters grouped in panel (`monitorPanelClass`).
- Deliver acceptance item: Filter chips reuse `filterChipClass`.

### Architecture refs

- docs/05_architecture.md — CLI and script invocation from app
- docs/08_environments.md — Local development commands
- docs/epics/EPIC-06.md — Epic scope and out-of-scope

### API / DB impact

- _n/a_ — no API or database impact expected.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Planned

- [x] **manual** — open Board; compare with Deliverables tab — same visual language.

## Record

### Files

- `src/features/monitor/components/KanbanView.tsx` — cards and columns with shadcn tokens
- `src/features/monitor/components/MonitorIssuesBanner.tsx` — issues panel aligned to monitor tokens
- `src/features/monitor/monitor-ui.tsx` — `monitorPanelClass`, `filterChipClass`

### Backend

- _n/a_

### Frontend

- Kanban cards/columns use `bg-card`, `border-border`, `text-foreground`.
- Filters grouped in a single `monitorPanelClass` panel; chips reuse `filterChipClass`.
- Issues banner matches the same panel and color tokens as the board.

### Scripts / Docs

- _n/a_

### Executed

- Manual: open Board; compare with Deliverables tab — same visual language.

## Boundaries

### Out of scope for this story

- Boundaries defined in `docs/epics/EPIC-06.md` and parent version `v1`.

### Notes

- Completed story — delivery record in `## Technical implementation`. Version: `v1`.
