---
id: US-0057
title: Native validate and sync board
epic: EPIC-07
version: v3
status: ❌
moscow: Must
depends_on:
  - US-0056
  - US-0044
done_when: Production Tauri build runs validate_meridian.py and generate_board.py without Vite dev API.
tests: required
tests_status: pending
ready: true
---

# US-0057 — Native validate and sync board

**As** Local Operator,
**I want** Validate folder and Sync Board in the shipped app,
**so that** I get the same governance as dev without pnpm dev.

## Acceptance

- [ ] Tauri command invokes Python scripts with project root containing docs/.
- [ ] Setup Advanced Tools validate button works in release build.
- [ ] Errors surface in UI same as v1 dev bridge.
- [ ] Vite-only POST /api/meridian/validate not required in Tauri mode.

## Context & constraints

### Why this story

This story adds **Native validate and sync board** — Validate folder and Sync Board in the shipped app. When done, I get the same governance as dev without pnpm dev. It is intentionally one slice; it does not deliver the whole epic alone.

### Where it fits

Release **v3** (`EPIC-07` in frontmatter — not duplicated here). Depends on US-0056 and US-0044 — read those US files for what must already work. Stops before: Extension commands (stay in VS Code).

### Approach

- Scope: Native validate and sync board under `v3`.
- Tauri command invokes Python scripts with project root containing docs/.
- Setup Advanced Tools validate button works in release build.
- Deliver acceptance item: Errors surface in UI same as v1 dev bridge.

### Architecture refs

- docs/05_architecture.md — Tauri shell and sidecar layout
- docs/01_tech_stack.md — Native wrapper decision
- docs/epics/EPIC-07.md — Epic scope and out-of-scope

### API / DB impact

- Documentation-only — no API or database changes.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

## Technical implementation

> On **creation**: placeholder below. On **completion** (`✅`): skill `complete-user-story` — replace with real record (files + layers).

### Files

_(fill in when implementation is complete)_

### Backend

_(fill in when applicable)_

### Frontend

_(fill in when applicable)_

### Scripts / Docs

_(fill in when applicable)_

## Tests

### Planned

- [ ] **manual** — 1. Tauri command invokes Python scripts with project root containing docs/.
- [ ] **manual** — 2. Setup Advanced Tools validate button works in release build.
- [ ] **manual** — 3. Errors surface in UI same as v1 dev bridge.

### Executed

_(pending)_

## Out of scope for this story

- Extension commands (stay in VS Code).

## Notes

- Roadmap US — implement only after prior version Must US are ✅ unless manager reprioritizes.
