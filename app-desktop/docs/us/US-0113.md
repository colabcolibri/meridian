---
id: US-0113
title: Document phase-doc vs database boundary
epic: EPIC-14
version: v9
status: ✅
moscow: Must
depends_on: [US-0105]
ready: true
done_when: "06_database.md and 05_architecture.md approved with explicit Markdown vs SQLite split."
tests: none
tests_status: n/a
---

# US-0113 — Document phase-doc vs database boundary

**As** Process Manager,
**I want** clear documentation of which artifacts stay Markdown and which live in SQLite,
**so that** agents and humans do not put phase gates in the database or delivery work in the wrong place.

## Intent

### Acceptance

- [x] `06_database.md` status → `approved` with: SQLite choice rationale, path `.meridian/meridian.db`, ER diagram (mermaid or table list), WAL mode note, migration/bootstrap commands.
- [x] `05_architecture.md` updated: Repository context tree shows `.meridian/meridian.db`; Layers table splits "Monitored project (phase .md)" vs "Delivery store (SQLite)".
- [x] Explicit inventory — **Markdown:** `00`–`11`, `discovery/`, `architecture/`, `inventory/`, `templates/`, `README.md`; **SQLite:** epics, versions, sprints, US, decisions, board_snapshots.
- [x] `.agent/references/start-here.md` gains one paragraph linking to storage split (kit change).
- [x] Manager sets `status: approved` on `06_database.md` frontmatter.

### Why

The user explicitly required that initial project docs remain `.md` while delivery artifacts migrate. Without a documented boundary, agents will continue writing `docs/us/*.md` or try to store scope in SQL. This US locks the contract before migration and kit rewrites proceed at scale.

### Where

**v9-S1**, depends on **US-0105** schema draft. Can proceed parallel to **US-0106**. Informs all v9-S2/S3 stories and agent behavior. Documentation-only — no product code.

## Plan

### Approach

- Update `06_database.md` sections: replace § Pending with § Schema (table list from migration file), § Bootstrap, § Migration, § What stays Markdown — copy ER from US-0105 migration SQL comments.
- Patch `05_architecture.md` § Repository context and § Layers only — do not rewrite VS Code extension sections; add note under Monitor views that delivery source becomes DB when `meridian.db` present.
- Add cross-link in `start-here.md` under `docs/` folder structure listing `.meridian/meridian.db`.

### Architecture refs

- `docs/05_architecture.md` — § Repository context
- `docs/05_architecture.md` — § Layers
- `docs/06_database.md` — full document update

### API / DB impact

- _n/a_ — documentation only.

### Security notes

- Document that `meridian.db` is gitignored and local-only.

### Related decisions

- Log storage-split decision on close via `/update-decisions-log`.

### Planned

- [x] **manual** — Manager reviews `06_database.md` and `05_architecture.md` diffs; sets `status: approved` on `06_database.md`.
- [x] **manual** — Confirm start-here paragraph mentions SQLite delivery store.

## Record

### Files

- `app-desktop/docs/06_database.md`
- `app-desktop/docs/05_architecture.md`
- `.agent/references/start-here.md`

### Backend

_n/a_

### Frontend

_n/a_

### Scripts / Docs

Phase vs SQLite boundary documented

### Executed

- 06_database.md status approved

## Boundaries

### Out of scope for this story

- Implementing schema or migration scripts (US-0105–0108).
- Updating every skill file — US-0111 covers kit skill text.
- Amending `00_scope.md` product scope section.

### Notes

- Can mark `06_database.md` approved before migration runs — schema is contractual.
