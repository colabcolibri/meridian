---
id: US-0007
title: Align .agent kit to Antigravity standard
epic: EPIC-01
version: v0
status: ✅
moscow: Must
depends_on:
  - US-0006
done_when: Root README.md, always_on rules, operational agents/skills/workflows, and meridian-routing exist in the kit.
tests: required
tests_status: done
---

# US-0007 — Align .agent kit to Antigravity standard

**As** Process Manager,
**I want** the Meridian kit to follow the same operational depth as Antigravity (rules, routing, references, workflows),
**so that** agents execute documentary governance with progressive disclosure and explicit routing.

## Acceptance

- [x] `README.md` at the repository root (Git convention).
- [x] `.agent/rules/MERIDIAN.md` with `trigger: always_on` and P0/P1/P2 priority.
- [x] Skill `meridian-routing` and guide `.agent/skills/doc.md`.
- [x] Skills with `allowed-tools`, selective reading table, and `references/`.
- [x] Agents expanded with phases, gates, prohibitions, and output format.
- [x] Workflows with `$ARGUMENTS`, critical rules, and CONTEXT block.
- [x] `.agent/ARCHITECTURE.md` documents differences vs. Antigravity.
- [x] `meridian.md` and `app-desktop/docs` reference the new structure.

## Context & constraints

### Why this story

This story delivered **Align .agent kit to Antigravity standard**: `README.md` at the repository root (Git convention).. It existed so that agents execute documentary governance with progressive disclosure and explicit routing. The record of what shipped lives under `## Technical implementation`.

### Where it fits

Release **v0** (`EPIC-01` in frontmatter — not duplicated here). Depends on US-0006 — read those US files for what must already work. Stops before: `npx` CLI to install the kit in external projects.

### Approach

- Delivered scope recorded under `## Technical implementation`.
- `README.md` at the repository root (Git convention).
- `.agent/rules/MERIDIAN.md` with `trigger: always_on` and P0/P1/P2 priority.
- Deliver acceptance item: Skill `meridian-routing` and guide `.agent/skills/doc.md`.

### Architecture refs

- docs/05_architecture.md — Monorepo layout and app-desktop shell
- docs/01_tech_stack.md — Vite, React, TypeScript, Tailwind
- docs/epics/EPIC-01.md — Epic scope and out-of-scope

### API / DB impact

- _n/a_ — no API or database impact expected.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

## Technical implementation

### Backend

- N/A

### Frontend

- `data.ts` includes US-0007 in simulation.
- App copy mentions `README.md` + `rules` always_on (optional).

## Tests

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

### Planned

- [x] **validate** — `python3 ../../.agent/scripts/validate_meridian.py .`
- [x] **lint** — `pnpm format:check`
- [x] **lint** — `pnpm lint`
- [x] **build** — `pnpm build`

### Executed

- `python3 ../../.agent/scripts/validate_meridian.py app-desktop` passed.
- `pnpm format:check`, `pnpm lint`, and `pnpm build` passed.

## Out of scope for this story

- `npx` CLI to install the kit in external projects.
- UI in the app to edit agents/workflows.

## Notes

- Reference: `antigravity-kit/.agent` adapted to Meridian's purpose (`docs/phase docs (00–08 and 11)`, US, derived board).
