---
id: US-0007
title: Alinhar kit .agent ao padrão Antigravity
epic: EPIC-01
version: v0
status: ✅
moscow: Must
depends_on:
  - US-0006
done_when: README.md na raiz, rules always_on, agents/skills/workflows
  operacionais e meridian-routing existem no kit.
tests: required
tests_status: done
---

# US-0007 — Alinhar kit `.agent` ao padrão Antigravity

**Como** Manager do Processo,
**quero** que o kit Meridian siga a mesma profundidade operacional do Antigravity (rules, routing, references, workflows),
**para que** agentes executem governança documental com progressive disclosure e roteamento explícito.

## Aceite

- [x] `README.md` na raiz do repositório do kit (convenção Git).
- [x] `.agent/rules/MERIDIAN.md` com `trigger: always_on` e prioridade P0/P1/P2.
- [x] Skill `meridian-routing` e guia `.agent/skills/doc.md`.
- [x] Skills com `allowed-tools`, tabela selective reading e `references/`.
- [x] Agents expandidos com fases, gates, proibições e formato de saída.
- [x] Workflows com `$ARGUMENTS`, regras críticas e bloco CONTEXT.
- [x] `.agent/ARCHITECTURE.md` documenta diferenças vs Antigravity.
- [x] `meridian.md` e `app-desktop/docs` referenciam a estrutura nova.

## Implementação técnica

### Backend

- N/A

### Frontend

- `data.ts` inclui US-0007 na simulação.
- Copy do app menciona `README.md` + `rules` always_on (opcional).

## Testes

> Na **criação**: preencher **Planejado**. Ao **fechar** (`complete-user-story`): marcar `[x]` e registrar em **Executado**.

### Planejado

- [x] **validate** — `python3 ../../.agent/scripts/validate_meridian.py .`
- [x] **lint** — `pnpm format:check`
- [x] **lint** — `pnpm lint`
- [x] **build** — `pnpm build`

### Executado

- `python3 ../../.agent/scripts/validate_meridian.py app-desktop` passou.
- `pnpm format:check`, `pnpm lint` e `pnpm build` passaram.

## Fora de escopo desta story

- CLI `npx` para instalar o kit em projetos externos.
- UI no app para editar agents/workflows.

## Notas

- Referência: `antigravity-kit/.agent` adaptado ao propósito Meridian (`docs/documentos de fase (00–08 e 11)`, US, board derivado).
