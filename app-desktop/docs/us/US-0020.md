---
id: US-0020
title: Simplified top bar
epic: EPIC-06
version: v1
status: ✅
moscow: Must
depends_on:
done_when: Single bar combines brand, opened folder, issue count, and actions; no three message bands.
tests: required
tests_status: done
ready: true
---

# US-0020 — Simplified top bar

**As** Process Manager,
**I want** to see project status in one line,
**so that** the screen doesn't look like a debug panel.

## Intent

### Acceptance

- [x] `MonitorTopBar` replaces `MonitorHeader` + `FolderToolbar`.
- [x] No `00_scope.md` / `us/` badges in the bar (silent validation).
- [x] Issue count only as pill when > 0.
- [x] Folder errors in single line below the bar.
- [x] Evidence: removed `MonitorHeader`, `FolderToolbar`, `OpenFolderPrompt`.

### Why

This story delivered **Simplified top bar**: `MonitorTopBar` replaces `MonitorHeader` + `FolderToolbar`.. It existed so that the screen doesn't look like a debug panel. The record of what shipped lives under `## Technical implementation`.

### Where

Release **v1** (`EPIC-06` in frontmatter — not duplicated here). Depends on US-0019 — read those US files for what must already work. Stops before: Boundaries defined in `docs/epics/EPIC-06.md` and parent version `v1`.

## Plan

### Approach

- Delivered scope recorded under `## Technical implementation`.
- `MonitorTopBar` replaces `MonitorHeader` + `FolderToolbar`.
- No `00_scope.md` / `us/` badges in the bar (silent validation).
- Deliver acceptance item: Issue count only as pill when > 0.

### Architecture refs

- docs/05_architecture.md — CLI and script invocation from app
- docs/08_environments.md — Local development commands
- docs/epics/EPIC-06.md — Epic scope and out-of-scope

### API / DB impact

- _n/a_ — no API or database impact expected.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Planned

- [x] **manual** — folder open → compact bar; switch/close folder.

## Record

### Files

- `app-desktop/src/features/monitor/components/MonitorTopBar.tsx` — compact folder bar
- `app-desktop/src/features/monitor/MonitorDashboard.tsx` — wires top bar and folder actions

### Backend

_n/a_

### Frontend

- `MonitorTopBar.tsx`; remove usage of legacy header/toolbar in dashboard.

### Scripts / Docs

_n/a_

### Executed

- Manual: folder open → compact bar; switch/close folder.

## Boundaries

### Out of scope for this story

- Boundaries defined in `docs/epics/EPIC-06.md` and parent version `v1`.

### Notes

- Completed story — delivery record in `## Technical implementation`. Version: `v1`.
