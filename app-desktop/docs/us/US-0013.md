---
id: US-0013
title: Kanban reads user stories and board.json
epic: EPIC-04
version: v1
status: ✅
moscow: Must
depends_on:
done_when: Kanban tab shows US from folder with status columns, epic filter, and done_when; board.json optional as derived cache.
tests: required
tests_status: done
ready: true
---

# US-0013 — Kanban reads user stories and board.json

**As** Process Manager,
**I want** a kanban of user stories linked to epics,
**so that** I prioritize work with agents without leaving the Meridian flow.

## Intent

### Acceptance

- [x] Columns: Pending (`❌`), In progress (`🔶`), Done (`✅`), Frozen (`🧊`).
- [x] Each card shows `id`, title, epic, version, MoSCoW, `done_when`, and dependencies.
- [x] Epic filter works with real data.
- [x] Primary source: `docs/us/US-*.md` via parser.
- [x] Compare with `board.json` and warn about divergences (status/epic and orphan entries).
- [x] US `🔶` without `Missing:` highlighted on card (via US-0014).

### Why

This story delivered **Kanban reads user stories and board.json**: Columns: Pending (`❌`), In progress (`🔶`), Done (`✅`), Frozen (`🧊`).. It existed so that I prioritize work with agents without leaving the Meridian flow. The record of what shipped lives under `## Technical implementation`.

### Where

Release **v1** (`EPIC-04` in frontmatter — not duplicated here). Depends on US-0010 — read those US files for what must already work. Stops before: Drag card to change status (v2).

## Plan

### Approach

- Primary files: `docs/kanban/board.json`.
- Columns: Pending (`❌`), In progress (`🔶`), Done (`✅`), Frozen (`🧊`).
- Each card shows `id`, title, epic, version, MoSCoW, `done_when`, and dependencies.
- Deliver acceptance item: Epic filter works with real data.

### Architecture refs

- docs/05_architecture.md — Monitor, kanban and project loader
- docs/epics/EPIC-04.md — Kanban and user story boundaries
- docs/epics/EPIC-04.md — Epic scope and out-of-scope

### API / DB impact

- Documentation-only — no API or database changes.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Planned

- [x] **automated** — `pnpm test` — `protocol-validators.test.ts` covers board diff.
- [x] **build** — `pnpm build` passed.

## Record

### Files

_(fill on close)_

### Backend

_(fill on close or \_n/a_)\_

### Frontend

- `KanbanView` fed by US parser.
- `compareBoardWithStories` in `protocol-validators.ts`.
- Reading `docs/kanban/board.json` in `project-loader.ts`.

### Scripts / Docs

_(fill on close or \_n/a_)\_

### Executed

- `pnpm test` — `protocol-validators.test.ts` covers board diff.
- `pnpm build` passed.

## Boundaries

### Out of scope for this story

- Drag card to change status (v2).
- Create US through the app.

### Notes

- Agents continue creating US in `docs/us/`; app only reads.
