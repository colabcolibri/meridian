---
id: US-0013
title: Kanban lê user stories e board.json
epic: EPIC-04
version: v1
status: ✅
moscow: Must
depends_on: [US-0010]
done_when: "Aba Kanban exibe US da pasta com colunas por status, filtro por epic e done_when; board.json opcional como cache derivado."
---

# US-0013 — Kanban lê user stories e board.json

**Como** Manager do Processo,
**quero** um kanban das user stories ligado aos épicos,
**para que** eu priorize o trabalho com agentes sem sair do fluxo Meridian.

## Aceite

- [x] Colunas: Pendente (`❌`), Em andamento (`🔶`), Concluída (`✅`), Congelada (`🧊`).
- [x] Cada card mostra `id`, título, epic, versão, MoSCoW, `done_when` e dependências.
- [x] Filtro por epic funciona com dados reais.
- [x] Fonte primária: `docs/us/US-*.md` via parser.
- [x] Comparar com `board.json` e avisar divergências (status/epic e entradas órfãs).
- [x] US `🔶` sem `Falta:` destacada no card (via US-0014).

## Implementação técnica

### Frontend

- `KanbanView` alimentado por parser de US.
- `compareBoardWithStories` em `protocol-validators.ts`.
- Leitura de `docs/kanban/board.json` em `project-loader.ts`.

## Testes

- `pnpm test` — `protocol-validators.test.ts` cobre diff de board.
- `pnpm build` passou.

## Fora de escopo desta story

- Arrastar card para mudar status (v2).
- Criar US pelo app.

## Notas

- Agents continuam criando US em `docs/us/`; app só lê.
