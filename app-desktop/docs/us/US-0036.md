---
id: US-0036
title: Focused Deliverables with flat layout
epic: EPIC-04
version: v1
status: ✅
moscow: Must
depends_on:
  - US-0034
done_when: Deliverables highlights global filter version; other versions collapsed; epics in flat rows.
tests: required
tests_status: done
---

# US-0036 — Focused Deliverables with flat layout

**As** Process Manager,
**I want** to see one version at a time in Deliverables without stacking v0/v1/v2,
**so that** I read progress without infinite scrolling through boxes.

## Acceptance

- [x] Global filter version appears highlighted at the top.
- [x] Other versions in a collapsible block "Other versions".
- [x] Epics in flat rows (no nested epic card).
- [x] Sprints in compact list (ID + short title).
- [x] Evidence: `DeliverablesView.tsx`.

## Context & constraints

### Architecture refs

- docs/05_architecture.md — Monitor, kanban and project loader
- docs/epics/EPIC-04.md — Kanban and user story boundaries
- docs/epics/EPIC-04.md — Epic scope and out-of-scope

### API / DB impact

- _n/a_ — no API or database impact expected.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Implementation hints (preliminary)

- Primary files: `src/features/monitor/components/DeliverablesView.tsx`.
- Global filter version appears highlighted at the top.
- Other versions in a collapsible block "Other versions".

## Tests

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

### Planned

- [x] **manual** — filter v1 — only v1 highlighted; v0/v2 inside "Other versions".

### Executed

- Manual: filter v1 — only v1 highlighted; v0/v2 inside "Other versions".

## Technical implementation

### Files

- `src/features/monitor/components/DeliverablesView.tsx` — focused version layout and collapsed others

### Backend

- _n/a_

### Frontend

- Global filter version highlighted at top; other versions in collapsible "Other versions" block.
- Epics shown as flat rows; sprints as compact ID + short title list.

### Scripts / Docs

- _n/a_

## Out of scope for this story

- Boundaries defined in `docs/epics/EPIC-04.md` and parent version `v1`.

## Notes

- Completed story — delivery record in `## Technical implementation`. Version: `v1`.
