---
id: US-0044
title: generate_board.py script in kit
epic: EPIC-05
version: v2
status: ❌
moscow: Must
depends_on:
  - US-0042
  - US-0043
done_when: .agent/scripts/generate_board.py reads docs/us/ and writes docs/kanban/board.json matching board-schema.
tests: required
tests_status: pending
---

# US-0044 — generate_board.py script in kit

**As** Process Manager,
**I want** a deterministic script to regenerate board.json,
**so that** the extension, CI, and agents share one implementation instead of fragile manual JSON edits.

## Acceptance

- [ ] New script `.agent/scripts/generate_board.py` accepts project root (folder containing `docs/`).
- [ ] Reads all `docs/us/US-*.md`, extracts frontmatter fields per `board-schema.md`.
- [ ] Validates epic/version refs; exits non-zero with clear errors on invalid US.
- [ ] Writes sorted `docs/kanban/board.json` (ascending US id).
- [ ] Output matches skill `generate-board-json` schema; documented in `.agent/MERIDIAN.md` or skill reference.
- [ ] `validate_meridian.py` optionally invokes or cross-checks script output.

## Technical implementation

> On **creation**: placeholder below. On **completion** (`✅`): skill `complete-user-story` — replace with real record (files + layers).

### Files

_(fill in when implementation is complete)_

### Backend

_(fill in when applicable)_

### Frontend

_(fill in when applicable)_

### Scripts / Docs

_(fill in when applicable)_

## Tests

### Planned

- [ ] **automated** — Run script on `app-desktop/` → board.json matches current US frontmatter.
- [ ] **automated** — Invalid US (bad epic ref) → script exits 1 with message.
- [ ] **manual** — Diff board before/after on dogfood docs → only expected fields change.

### Executed

_(pending)_

## Out of scope for this story

- VS Code command wiring (US-0045).
- TypeScript duplicate of parser in extension.

## Notes

- Fills gap: today board sync is agent-only via skill; v2 requires callable script.
