---
id: US-0011
title: Configuração inicial com docs reais
epic: EPIC-02
version: v1
status: ✅
moscow: Must
depends_on: [US-0010]
done_when: "Aba Configuração inicial reflete docs/00-11 da pasta aberta; progresso e alertas batem com frontmatter."
---

# US-0011 — Configuração inicial com docs reais

**Como** Manager do Processo,
**quero** ver o progresso dos documentos 00–11 da pasta que abri,
**para que** eu saiba o que aprovar ou corrigir antes de pedir código aos agentes.

## Aceite

- [x] Aba Configuração inicial usa apenas arquivos lidos da pasta (sem `phaseDocuments` mock).
- [x] Contador X/Y de etapas concluídas reflete `status: approved` com deps satisfeitas.
- [x] Estado **Requer correção** aparece quando doc está `approved` e predecessores não estão `approved`.
- [x] Próximo passo sugerido aponta para o primeiro doc em elaboração com deps ok.
- [x] Abrir `app-desktop/docs/` mostra estados reais dos docs 00–11 (ex.: `07` approved após v1).

## Implementação técnica

### Frontend

- `ProjectDataProvider` recarrega ao abrir pasta.
- `MonitorDashboard` alimenta `SetupMonitorView` com `data.phaseDocuments` parseados.

## Testes

- `pnpm build` passou.
- Dogfooding: abrir pasta `app-desktop/docs/`, aba Configuração inicial com estados reais.

## Fora de escopo desta story

- Leitura inline do markdown (US-0017 — já entrega visualização do `.md`).
- Comparação com `board.json` (US-0013).
- Corrigir automaticamente inconsistências nos arquivos.

## Notas

- US-0002 pode ser marcada ✅ (leitura real + lista).
