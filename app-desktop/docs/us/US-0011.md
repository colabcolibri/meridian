---
id: US-0011
title: Initial setup with real docs
epic: EPIC-02
version: v1
status: ✅
moscow: Must
depends_on:
  - US-0010
done_when: Setup tab reflects phase documents from opened folder; progress and alerts match frontmatter.
tests: required
tests_status: done
---

# US-0011 — Initial setup with real docs

**As** Process Manager,
**I want** to see the progress of phase documents (00–08 and 11) from the folder I opened,
**so that** I know what to approve or fix before asking agents to code.

## Acceptance

- [x] Initial setup tab uses only files read from the folder (no mock `phaseDocuments`).
- [x] X/Y completed steps counter reflects `status: approved` with satisfied dependencies.
- [x] **Requires correction** state appears when a doc is `approved` and predecessors are not `approved`.
- [x] Suggested next step points to the first doc in progress with satisfied dependencies.
- [x] Opening `app-desktop/docs/` shows real states of phase documents (e.g. `05_architecture` approved).

## Context & constraints

### Architecture refs

- docs/05_architecture.md — Project folder access and docs/ layout
- docs/04_principles.md — Docs-first and local-first operation
- docs/epics/EPIC-02.md — Epic scope and out-of-scope

### API / DB impact

- Documentation-only — no API or database changes.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Implementation hints (preliminary)

- Delivered scope recorded under `## Technical implementation`.
- Initial setup tab uses only files read from the folder (no mock `phaseDocuments`).
- X/Y completed steps counter reflects `status: approved` with satisfied dependencies.

## Technical implementation

### Frontend

- `ProjectDataProvider` reloads on folder open.
- `MonitorDashboard` feeds `SetupMonitorView` with parsed `data.phaseDocuments`.

## Tests

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

### Planned

- [x] **build** — `pnpm build` passed.
- [x] **dogfooding** — Open `app-desktop/docs/` folder, Initial setup tab with real states.

### Executed

- `pnpm build` passed.
- Dogfooding: open `app-desktop/docs/` folder, Initial setup tab with real states.

## Out of scope for this story

- Inline Markdown reading (US-0017 — already delivers `.md` visualization).
- Comparison with `board.json` (US-0013).
- Automatically fix file inconsistencies.

## Notes

- US-0002 can be marked ✅ (real reading + list).
