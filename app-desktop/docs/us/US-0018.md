---
id: US-0018
title: Onboarding to open docs folder
epic: EPIC-06
version: v1
status: ✅
moscow: Must
depends_on:
  - US-0009
done_when: First use shows clear steps and a single primary CTA to open docs folder, without duplicated technical messages.
tests: required
tests_status: done
---

# US-0018 — Onboarding to open docs folder

**As** Process Manager,
**I want** to be guided on the first app open,
**so that** I know exactly which folder to choose without reading the README.

## Acceptance

- [x] Initial screen without folder: benefit-oriented title (no API jargon).
- [x] Three numbered steps (what docs is, what happens when opening, supported browser).
- [x] One highlighted primary button "Open project folder".
- [x] Evidence: `WelcomeScreen.tsx`, `pnpm build`.

## Context & constraints

### Why this story

This story delivered **Onboarding to open docs folder**: Initial screen without folder: benefit-oriented title (no API jargon).. It existed so that I know exactly which folder to choose without reading the README. The record of what shipped lives under `## Technical implementation`.

### Where it fits

Release **v1** (`EPIC-06` in frontmatter — not duplicated here). Depends on US-0009 — read those US files for what must already work. Stops before: Tour after folder is open (v1-S2+).

### Approach

- Delivered scope recorded under `## Technical implementation`.
- Initial screen without folder: benefit-oriented title (no API jargon).
- Three numbered steps (what docs is, what happens when opening, supported browser).
- Deliver acceptance item: One highlighted primary button "Open project folder".

### Architecture refs

- docs/05_architecture.md — CLI and script invocation from app
- docs/08_environments.md — Local development commands
- docs/epics/EPIC-06.md — Epic scope and out-of-scope

### API / DB impact

- Review `docs/07_api_contracts.md` before implementation.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

## Technical implementation

### Frontend

- `WelcomeScreen.tsx` replaces `OpenFolderPrompt` when no folder is open.

## Tests

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

### Planned

- [x] **manual** — open app without folder → see onboarding → open `app-desktop/docs/`.

### Executed

- Manual: open app without folder → see onboarding → open `app-desktop/docs/`.

## Out of scope for this story

- Tour after folder is open (v1-S2+).

## Notes

- Depends on US-0009 (File System Access).
