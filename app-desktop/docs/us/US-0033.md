---
id: US-0033
title: EPIC-06 complete — visually unified monitor
epic: EPIC-06
version: v1
status: ✅
moscow: Must
depends_on:
  - US-0031
  - US-0032
done_when: No zinc-* in src/features/monitor/; EPIC-06 marked complete.
tests: required
tests_status: done
---

# US-0033 — EPIC-06 complete — visually unified monitor

**As** Process Manager,
**I want** all monitor tabs with a coherent visual,
**so that** the v1 experience feels like a finished product, not a prototype.

## Acceptance

- [x] Zero occurrences of `zinc-*` in `src/features/monitor/`.
- [x] Get Started (`ConceptsView`) uses `monitorPanelClass` on main blocks.
- [x] EPIC-06 status `complete` in `docs/epics/EPIC-06.md`.
- [x] Evidence: sprint v1-S5 + grep in code.

## Context & constraints

### Architecture refs

- docs/05_architecture.md — CLI and script invocation from app
- docs/08_environments.md — Local development commands
- docs/epics/EPIC-06.md — Epic scope and out-of-scope

### API / DB impact

- Documentation-only — no API or database changes.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Implementation hints (preliminary)

- Primary files: `src/features/monitor/components/ConceptsView.tsx`, `docs/epics/EPIC-06.md`.
- Zero occurrences of `zinc-*` in `src/features/monitor/`.
- Get Started (`ConceptsView`) uses `monitorPanelClass` on main blocks.

## Tests

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

### Planned

- [x] **manual** — go through the 4 tabs — consistent colors and borders.

### Executed

- Manual: go through the 4 tabs — consistent colors and borders.

## Technical implementation

### Files

- `src/features/monitor/components/ConceptsView.tsx` — Get Started blocks with `monitorPanelClass`
- `docs/epics/EPIC-06.md` — epic marked `status: complete`

### Backend

- _n/a_

### Frontend

- Zero `zinc-*` classes under `src/features/monitor/` (verified by grep).
- Get Started guide uses the same panel tokens as operational tabs.

### Scripts / Docs

- `docs/epics/EPIC-06.md` — EPIC-06 closure recorded after US-0031 and US-0032.

## Out of scope for this story

- Boundaries defined in `docs/epics/EPIC-06.md` and parent version `v1`.

## Notes

- Completed story — delivery record in `## Technical implementation`. Version: `v1`.
