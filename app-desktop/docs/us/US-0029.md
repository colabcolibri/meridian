---
id: US-0029
title: Deliverables with cards and progress bar
epic: EPIC-06
version: v1
status: ✅
moscow: Must
depends_on:
  - US-0025
done_when: Each version in card with highlighted header, progress % and visual bar; epics with mini progress.
tests: required
tests_status: done
---

# US-0029 — Deliverables with cards and progress bar

**As** Process Manager,
**I want** to see version and epic progress at a glance,
**so that** I know where the release stands without manually counting US.

## Acceptance

- [x] Version in `Card` with `meridian-muted` header and outcome.
- [x] Version progress bar (`done/total` + %).
- [x] Each epic listed with its own progress bar.
- [x] Intro in panel consistent with the board.
- [x] Evidence: `DeliverablesView.tsx`, `ProgressBar` in `monitor-ui.tsx`.

## Context & constraints

### Architecture refs

- docs/05_architecture.md — CLI and script invocation from app
- docs/08_environments.md — Local development commands
- docs/epics/EPIC-06.md — Epic scope and out-of-scope

### API / DB impact

- _n/a_ — no API or database impact expected.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Implementation hints (preliminary)

- Primary files: `src/features/monitor/components/DeliverablesView.tsx`, `src/features/monitor/monitor-ui.tsx`.
- Version in `Card` with `meridian-muted` header and outcome.
- Version progress bar (`done/total` + %).

## Tests

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

### Planned

- [x] **manual** — Deliverables tab — v1 shows filled bar according to completed US.

### Executed

- Manual: Deliverables tab — v1 shows filled bar according to completed US.

## Technical implementation

### Files

- `src/features/monitor/components/DeliverablesView.tsx` — version cards, epic rows, progress display
- `src/features/monitor/monitor-ui.tsx` — shared `ProgressBar` component

### Backend

- _n/a_

### Frontend

- Each version rendered in a `Card` with `meridian-muted` header and outcome text.
- Version and epic progress show `done/total`, percentage, and visual bar.
- Intro panel uses the same monitor panel styling as the board.

### Scripts / Docs

- _n/a_

## Out of scope for this story

- Boundaries defined in `docs/epics/EPIC-06.md` and parent version `v1`.

## Notes

- Completed story — delivery record in `## Technical implementation`. Version: `v1`.
