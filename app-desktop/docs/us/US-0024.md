---
id: US-0024
title: Kanban com badge de versão e filtro
epic: EPIC-04
version: v1
status: ✅
moscow: Must
depends_on: [US-0022]
done_when: "Cada card mostra badge da versão; quadro filtra por v0/v1/v2."
---

# US-0024 — Kanban com badge de versão e filtro

**Como** Manager do Processo,
**quero** filtrar o quadro por versão de produto,
**para que** eu veja só as US do release atual.

## Aceite

- [x] Badge `vX` visível no topo de cada card (não só em `<details>`).
- [x] Filtro por versão acima das colunas (Todas + cada `version` das US).
- [x] Filtro de versão combina com filtro de epic.
- [x] Evidência: `KanbanView.tsx` — `versionFilter` e badge no `StoryCard`.

## Implementação técnica

### Frontend

- `KanbanView.tsx` — estado `versionFilter` e badges.

## Testes

- Manual: filtrar `v1`; confirmar que US de v0 somem das colunas.
