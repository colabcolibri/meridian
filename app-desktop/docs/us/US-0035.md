---
id: US-0035
title: Epic filter cascaded by version
epic: EPIC-04
version: v1
status: ✅
moscow: Must
depends_on:
  - US-0034
done_when: Epic chips on Board list only epics with US in selected version; epic resets when version changes.
tests: required
tests_status: done
---

# US-0035 — Epic filter cascaded by version

**As** Process Manager,
**I want** to see only relevant epics when filtering a version,
**so that** chips below the filter don't show EPICs from another release.

## Acceptance

- [x] With version v1, epic chips don't include epics only from v0.
- [x] When changing version, epic filter resets to All.
- [x] Version badge on card disappears when a specific version is selected.
- [x] Evidence: `KanbanView.tsx`, `version-filter.ts`.

## Context & constraints

### Why this story

This story delivered **Epic filter cascaded by version**: With version v1, epic chips don't include epics only from v0.. It existed so that chips below the filter don't show EPICs from another release. The record of what shipped lives under `## Technical implementation`.

### Where it fits

Release **v1** (`EPIC-04` in frontmatter — not duplicated here). Depends on US-0034 — read those US files for what must already work. Stops before: Boundaries defined in `docs/epics/EPIC-04.md` and parent version `v1`.

### Approach

- Primary files: `src/features/monitor/components/KanbanView.tsx`, `src/features/monitor/version-filter.ts`.
- With version v1, epic chips don't include epics only from v0.
- When changing version, epic filter resets to All.
- Deliver acceptance item: Version badge on card disappears when a specific version is selected.

### Architecture refs

- docs/05_architecture.md — Monitor, kanban and project loader
- docs/epics/EPIC-04.md — Kanban and user story boundaries
- docs/epics/EPIC-04.md — Epic scope and out-of-scope

### API / DB impact

- _n/a_ — no API or database impact expected.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

## Tests

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

### Planned

- [x] **manual** — select v1 — confirm that EPIC-01 doesn't appear in chips.

### Executed

- Manual: select v1 — confirm that EPIC-01 doesn't appear in chips.

## Technical implementation

### Files

- `src/features/monitor/components/KanbanView.tsx` — cascaded epic chips and version badge visibility
- `src/features/monitor/version-filter.ts` — `epicsForVersionFilter`, `filterStoriesByVersions`

### Backend

- _n/a_

### Frontend

- Epic chips list only epics with US in the selected version(s).
- Changing version resets epic filter to All; version badge hidden when a specific version is selected.

### Scripts / Docs

- _n/a_

## Out of scope for this story

- Boundaries defined in `docs/epics/EPIC-04.md` and parent version `v1`.

## Notes

- Completed story — delivery record in `## Technical implementation`. Version: `v1`.
