---
id: US-015
title: Invocar validate_meridian.py na pasta
epic: EPIC-03
version: v1
status: ✅
moscow: Should
depends_on: [US-014]
done_when: "App dispara validação do script na pasta aberta e exibe erros/avisos na UI."
---

# US-015 — Invocar validate_meridian.py na pasta

**Como** Operador Local,
**quero** rodar a mesma validação que o kit usa nos agents,
**para que** app e Cursor vejam os mesmos problemas na pasta monitorada.

## Aceite

- [x] Ação **Validar pasta** chama `validate_meridian.py` via `POST /api/meridian/validate` (bridge Vite em dev).
- [x] Saída (erros/avisos) renderizada em painel legível.
- [x] Falha do script (Python ausente) exibe instrução alinhada a `10_environments.md`.
- [x] Resultado alinhado ao terminal para `app-desktop/` em dev.
- [x] Build produção: botão desabilitado + comando para copiar no terminal.

## Implementação técnica

### Frontend

- `vite-meridian-validate.ts` — middleware dev/preview.
- `ScriptValidationPanel.tsx`, `run-validate-script.ts`.

### Documentação

- `07_architecture.md` — bridge documentada.
- `10_environments.md` — Python 3 e comando de validação.

## Testes

- `pnpm build` passou.
- Manual: `pnpm dev` → Abrir pasta docs → Validar pasta (script em `app-desktop/` no disco).

## Fora de escopo desta story

- Executar Python no browser sem dev server.
- Substituir totalmente o script por TS.

## Notas

- Em dev, pasta validada é sempre `app-desktop/` no disco (limitação FSA).
