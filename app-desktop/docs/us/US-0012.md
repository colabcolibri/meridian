---
id: US-0012
title: Aba Entregas lê docs/epics/
epic: EPIC-04
version: v1
status: ✅
moscow: Must
depends_on:
  - US-0010
done_when: Aba Entregas lista EPIC-XX da pasta aberta com descrição, versões,
  status e contagem de US do mesmo projeto.
tests: required
tests_status: done
---

# US-0012 — Aba Entregas lê docs/epics/

**Como** Manager do Processo,
**quero** ver os épicos definidos em `docs/epics/`,
**para que** eu alinhe prioridades de produto com o backlog de user stories.

## Aceite

- [x] Cards gerados a partir de `docs/epics/EPIC-XX.md` parseados (não array fixo em `data.ts`).
- [x] Cada epic mostra descrição, versões, status active/paused/complete e contagem de US (`docs/us/`).
- [x] Epics pausados (ex.: EPIC-05) aparecem em seção separada.
- [x] Epics concluídos (ex.: EPIC-01) em seção **Concluídos**.
- [x] Alterar um arquivo em `docs/epics/` e recarregar pasta atualiza a aba.

## Implementação técnica

### Frontend

- `DeliverablesView` / `EpicsView` recebe `Epic[]` do parser + agregação de US.

## Testes

> Na **criação**: preencher **Planejado**. Ao **fechar** (`complete-user-story`): marcar `[x]` e registrar em **Executado**.

### Planejado

- [x] **build** — `pnpm build` passou.
- [x] **dogfooding** — Dogfooding com pasta `app-desktop/docs/` aberta.

### Executado

- `pnpm build` passou.
- Dogfooding com pasta `app-desktop/docs/` aberta.

## Fora de escopo desta story

- Criar ou editar epics pelo app (v2 / editor).
- Kanban (US-0013).

## Notas

- Registro canônico de epics permanece só em Markdown em `docs/epics/`.
