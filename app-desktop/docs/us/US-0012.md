---
id: US-0012
title: Deliverables tab reads docs/epics/
epic: EPIC-04
version: v1
status: ✅
moscow: Must
depends_on:
done_when: Deliverables tab lists EPIC-XX from opened folder with description, versions, status, and US count from same project.
tests: required
tests_status: done
ready: true
---

# US-0012 — Deliverables tab reads docs/epics/

**As** Process Manager,
**I want** to see the epics defined in `docs/epics/`,
**so that** I align product priorities with the user story backlog.

## Intent

### Acceptance

- [x] Cards generated from parsed `docs/epics/EPIC-XX.md` (not a fixed array in `data.ts`).
- [x] Each epic shows description, versions, active/paused/complete status, and US count (`docs/us/`).
- [x] Paused epics (e.g. EPIC-05) appear in a separate section.
- [x] Completed epics (e.g. EPIC-01) in a **Completed** section.
- [x] Changing a file in `docs/epics/` and reloading folder updates the tab.

### Why

This story delivered **Deliverables tab reads docs/epics/**: Cards generated from parsed `docs/epics/EPIC-XX.md` (not a fixed array in `data.ts`).. It existed so that I align product priorities with the user story backlog. The record of what shipped lives under `## Technical implementation`.

### Where

Release **v1** (`EPIC-04` in frontmatter — not duplicated here). Depends on US-0010 — read those US files for what must already work. Stops before: Create or edit epics through the app (v2 / editor).

## Plan

### Approach

- Delivered scope recorded under `## Technical implementation`.
- Cards generated from parsed `docs/epics/EPIC-XX.md` (not a fixed array in `data.ts`).
- Each epic shows description, versions, active/paused/complete status, and US count (`docs/us/`).
- Deliver acceptance item: Paused epics (e.g. EPIC-05) appear in a separate section.

### Architecture refs

- docs/05_architecture.md — Monitor, kanban and project loader
- docs/epics/EPIC-04.md — Kanban and user story boundaries
- docs/epics/EPIC-04.md — Epic scope and out-of-scope

### API / DB impact

- Documentation-only — no API or database changes.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Planned

- [x] **build** — `pnpm build` passed.
- [x] **dogfooding** — Dogfooding with `app-desktop/docs/` folder open.

## Record

### Files

_(fill on close)_

### Backend

_(fill on close or \_n/a_)\_

### Frontend

- `DeliverablesView` / `EpicsView` receives `Epic[]` from parser + US aggregation.

### Scripts / Docs

_(fill on close or \_n/a_)\_

### Executed

- `pnpm build` passed.
- Dogfooding with `app-desktop/docs/` folder open.

## Boundaries

### Out of scope for this story

- Create or edit epics through the app (v2 / editor).
- Kanban (US-0013).

### Notes

- The canonical epic record remains only in Markdown in `docs/epics/`.
