---
id: US-014
title: Validações de protocolo na UI
epic: EPIC-03
version: v1
status: ✅
moscow: Must
depends_on: [US-011, US-013]
done_when: "App destaca inconsistências de docs e US conforme regras P0 do MERIDIAN.md na pasta aberta."
---

# US-014 — Validações de protocolo na UI

**Como** Manager do Processo,
**quero** alertas visuais quando a pasta viola o protocolo Meridian,
**para que** eu corrija documentação antes de agentes implementarem errado.

## Aceite

- [x] Doc dependente `approved` com predecessor não `approved` → alerta na Configuração inicial.
- [x] US `🔶` sem linha `Falta:` no aceite → alerta no Kanban.
- [x] US `✅` sem evidência registrada (seção Testes vazia) → aviso visível.
- [x] Painel ou badge **N problemas** na shell quando pasta aberta.
- [x] Regras implementadas em funções puras (`protocol-validators.ts`, `story-body.ts`).

## Implementação técnica

### Frontend

- `collectProtocolIssues`, `MonitorIssuesBanner`, badge na `FolderToolbar`.
- Alertas inline em `SetupMonitorView` e `KanbanView`.

## Testes

- `pnpm test` — `protocol-validators.test.ts`.
- `pnpm build` passou.
- Dogfooding: `10_environments` approved com `07` draft gera alerta.

## Fora de escopo desta story

- Executar script Python (US-015).
- Auto-corrigir arquivos.

## Notas

- EPIC-03 entregue na UI para leitura; script Python permanece US-015.
