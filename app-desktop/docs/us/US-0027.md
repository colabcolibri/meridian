---
id: US-0027
title: Validate and expose versions and sprints in UI
epic: EPIC-03
version: v1
status: ✅
moscow: Must
depends_on:
  - US-0014
  - US-0017
done_when: Loader reads docs/versions/ and docs/sprints/; alerts cite file; Deliverables shows sprints per version.
tests: required
tests_status: done
---

# US-0027 — Validate and expose versions and sprints in UI

**As** Process Manager,
**I want** to see sprints and releases in the monitor and alerts with file path,
**so that** I fix `docs/versions/` and `docs/sprints/` with clear context.

## Acceptance

- [x] `project-loader.ts` loads `docs/versions/` and `docs/sprints/` (warnings if absent).
- [x] `protocol-validators.ts` validates version/sprint references against catalog.
- [x] Issues banner shows `file · target — message`.
- [x] Deliverables shows sprint badges per version (`DeliverablesView`).
- [x] Evidence: `MonitorIssuesBanner.tsx`, `DeliverablesView.tsx`, parser tests.

## Context & constraints

### Why this story

This story delivered **Validate and expose versions and sprints in UI**: `project-loader.ts` loads `docs/versions/` and `docs/sprints/` (warnings if absent).. It existed so that I fix `docs/versions/` and `docs/sprints/` with clear context. The record of what shipped lives under `## Technical implementation`.

### Where it fits

Release **v1** (`EPIC-03` in frontmatter — not duplicated here). Depends on US-0014 and US-0017 — read those US files for what must already work. Stops before: Boundaries defined in `docs/epics/EPIC-03.md` and parent version `v1`.

### Approach

- Delivered scope recorded under `## Technical implementation`.
- `project-loader.ts` loads `docs/versions/` and `docs/sprints/` (warnings if absent).
- `protocol-validators.ts` validates version/sprint references against catalog.
- Deliver acceptance item: Issues banner shows `file · target — message`.

### Architecture refs

- docs/05_architecture.md — Phase documents and governance flow
- docs/11_decisions.md — Decision log structure
- docs/epics/EPIC-03.md — Epic scope and out-of-scope

### API / DB impact

- Documentation-only — no API or database changes.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

## Technical implementation

### Domain + UI

- Loader, validators, and `DeliverablesView` / `MonitorIssuesBanner`.

## Tests

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

### Planned

- [x] **automated** — `pnpm test` — parser for `versions/v1.md` and `sprints/v1-S1.md`.
- [x] **manual** — banner with path `us/US-XXXX.md` or `versions/`.

### Executed

- `pnpm test` — parser for `versions/v1.md` and `sprints/v1-S1.md`.
- Manual: banner with path `us/US-XXXX.md` or `versions/`.

## Out of scope for this story

- Boundaries defined in `docs/epics/EPIC-03.md` and parent version `v1`.

## Notes

- Completed story — delivery record in `## Technical implementation`. Version: `v1`.
