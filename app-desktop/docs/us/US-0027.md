---
id: US-0027
title: Validate and expose versions and sprints in UI
epic: EPIC-03
version: v1
status: ✅
moscow: Must
depends_on:
  - US-0014
  - US-0017
done_when: Loader reads docs/versions/ and docs/sprints/; alerts cite file; Deliverables shows sprints per version.
tests: required
tests_status: done
---

# US-0027 — Validate and expose versions and sprints in UI

**As** Process Manager,
**I want** to see sprints and releases in the monitor and alerts with file path,
**so that** I fix `docs/versions/` and `docs/sprints/` with clear context.

## Acceptance

- [x] `project-loader.ts` loads `docs/versions/` and `docs/sprints/` (warnings if absent).
- [x] `protocol-validators.ts` validates version/sprint references against catalog.
- [x] Issues banner shows `file · target — message`.
- [x] Deliverables shows sprint badges per version (`DeliverablesView`).
- [x] Evidence: `MonitorIssuesBanner.tsx`, `DeliverablesView.tsx`, parser tests.

## Technical implementation

### Domain + UI

- Loader, validators, and `DeliverablesView` / `MonitorIssuesBanner`.

## Tests

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

### Planned

- [x] **automated** — `pnpm test` — parser for `versions/v1.md` and `sprints/v1-S1.md`.
- [x] **manual** — banner with path `us/US-XXXX.md` or `versions/`.

### Executed

- `pnpm test` — parser for `versions/v1.md` and `sprints/v1-S1.md`.
- Manual: banner with path `us/US-XXXX.md` or `versions/`.
