---
id: US-0001
title: Open Meridian app locally
epic: EPIC-01
version: v0
status: ✅
moscow: Must
depends_on: []
done_when: App runs with pnpm dev and renders a professional initial layout.
tests: required
tests_status: done
---

# US-0001 — Open Meridian app locally

**As** Process Manager,
**I want** to open the Meridian app locally,
**so that** I can start operating the development flow with visibility.

## Acceptance

- Vite app created with React and TypeScript.
- App runs with `pnpm dev`.
- Initial screen renders without error.
- Initial layout uses a professional structure compatible with shadcn/ui.

## Context & constraints

### Architecture refs

- docs/05_architecture.md — Monorepo layout and app-desktop shell
- docs/01_tech_stack.md — Vite, React, TypeScript, Tailwind
- docs/epics/EPIC-01.md — Epic scope and out-of-scope

### API / DB impact

- _n/a_ — no API or database impact expected.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Implementation hints (preliminary)

- Delivered scope recorded under `## Technical implementation`.
- Vite app created with React and TypeScript.
- App runs with `pnpm dev`.

## Technical implementation

### Frontend

- Create Vite app.
- Configure TypeScript.
- Configure Tailwind CSS.
- Prepare shadcn/ui base.
- Create Meridian initial layout.

## Tests

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

### Planned

- [x] **build** — `pnpm build` passed.
- [x] **manual** — `pnpm dev` validated locally.

### Executed

- `pnpm build` passed.
- `pnpm dev` validated locally.

## Out of scope for this story

- Real writes to Markdown files.
- Visual Studio/VSCode extension.
- Backend.

## Notes

- This US belongs to v0 Foundation.
