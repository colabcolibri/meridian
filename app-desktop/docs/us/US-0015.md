---
id: US-0015
title: Invoke validate_meridian.py on folder
epic: EPIC-03
version: v1
status: ✅
moscow: Should
depends_on:
  - US-0014
done_when: App triggers script validation on opened folder and shows errors/warnings in UI.
tests: required
tests_status: done
---

# US-0015 — Invoke validate_meridian.py on folder

**As** Local Operator,
**I want** to run the same validation that the kit uses in agents,
**so that** the app and Cursor see the same issues in the monitored folder.

## Acceptance

- [x] **Validate folder** action calls `validate_meridian.py` via `POST /api/meridian/validate` (Vite bridge in dev).
- [x] Output (errors/warnings) rendered in a readable panel.
- [x] Script failure (Python absent) displays instruction aligned to `08_environments.md`.
- [x] Result aligned to terminal for `app-desktop/` in dev.
- [x] Production build: button disabled + command to copy in terminal.

## Context & constraints

### Why this story

This story delivered **Invoke validate_meridian.py on folder**: **Validate folder** action calls `validate_meridian.py` via `POST /api/meridian/validate` (Vite bridge in dev).. It existed so that the app and Cursor see the same issues in the monitored folder. The record of what shipped lives under `## Technical implementation`.

### Where it fits

Release **v1** (`EPIC-03` in frontmatter — not duplicated here). Depends on US-0014 — read those US files for what must already work. Stops before: Run Python in the browser without dev server.

### Approach

- Delivered scope recorded under `## Technical implementation`.
- **Validate folder** action calls `validate_meridian.py` via `POST /api/meridian/validate` (Vite bridge in dev).
- Output (errors/warnings) rendered in a readable panel.
- Deliver acceptance item: Script failure (Python absent) displays instruction aligned to `08_environments.md`.

### Architecture refs

- docs/05_architecture.md — Phase documents and governance flow
- docs/11_decisions.md — Decision log structure
- docs/epics/EPIC-03.md — Epic scope and out-of-scope

### API / DB impact

- Documentation-only — no API or database changes.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

## Technical implementation

### Frontend

- `vite-meridian-validate.ts` — dev/preview middleware.
- `ScriptValidationPanel.tsx`, `run-validate-script.ts`.

### Documentation

- `05_architecture.md` — bridge documented.
- `08_environments.md` — Python 3 and validation command.

## Tests

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

### Planned

- [x] **build** — `pnpm build` passed.
- [x] **manual** — `pnpm dev` → Open docs folder → Validate folder (script on `app-desktop/` on disk).

### Executed

- `pnpm build` passed.
- Manual: `pnpm dev` → Open docs folder → Validate folder (script on `app-desktop/` on disk).

## Out of scope for this story

- Run Python in the browser without dev server.
- Fully replace the script with TypeScript.

## Notes

- In dev, the validated folder is always `app-desktop/` on disk (FSA limitation).
