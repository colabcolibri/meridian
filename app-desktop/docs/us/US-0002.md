---
id: US-0002
title: View phase documents and status
epic: EPIC-02
version: v0
status: ✅
moscow: Must
depends_on:
done_when: Document list shows status, dependencies, and basic blockers.
tests: required
tests_status: done
ready: true
---

# US-0002 — View phase documents and status

**As** Process Manager,
**I want** to view phase documents and their statuses,
**so that** I know what is ready, blocked, or pending before moving forward.

## Intent

### Acceptance

- [x] App lists documents `00` to `11` from the opened folder.
- [x] Each document shows a single readable state (no confusing double column).
- [x] Dependencies and next step are visible on the Initial setup tab.
- [x] Data comes from `docs/*.md` after opening folder (US-0009–011).
- [ ] The canonical board remains in `docs/kanban/board.json` (kanban on dedicated tab).

### Why

This story delivered **View phase documents and status**: App lists documents `00` to `11` from the opened folder.. It existed so that I know what is ready, blocked, or pending before moving forward. The record of what shipped lives under `## Technical implementation`.

### Where

Release **v0** (`EPIC-02` in frontmatter — not duplicated here). Depends on US-0001 — read those US files for what must already work. Stops before: Real Markdown editing.

## Plan

### Approach

- Delivered scope recorded under `## Technical implementation`.
- App lists documents `00` to `11` from the opened folder.
- Each document shows a single readable state (no confusing double column).
- Deliver acceptance item: Dependencies and next step are visible on the Initial setup tab.

### Architecture refs

- docs/05_architecture.md — Project folder access and docs/ layout
- docs/04_principles.md — Docs-first and local-first operation
- docs/epics/EPIC-02.md — Epic scope and out-of-scope

### API / DB impact

- Documentation-only — no API or database changes.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Planned

- [x] **build** — `pnpm build` passed.
- [x] **manual** — Listing and visual states on Setup tab (real reading from US-0009).

## Record

### Files

_(fill on close)_

### Backend

_(fill on close or \_n/a_)\_

### Frontend

- Create initial Meridian flow data in TypeScript.
- Create document panel.
- Create status badges.
- Create basic dependency blocking calculation.

### Scripts / Docs

_(fill on close or \_n/a_)\_

### Executed

- `pnpm build` passed.
- Manual: Setup tab lists phase docs (00–08 and 11) with states and dependencies.

## Boundaries

### Out of scope for this story

- Real Markdown editing.
- Complete frontmatter validations.
- CSV export.

### Notes

- This US starts the Meridian dashboard visual experience.
