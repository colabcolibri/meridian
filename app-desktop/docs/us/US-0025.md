---
id: US-0025
title: Deliverables with version → epic hierarchy
epic: EPIC-04
version: v1
status: ✅
moscow: Must
depends_on:
done_when: Deliverables tab groups by version, lists epics with US count and version sprints.
tests: required
tests_status: done
ready: true
---

# US-0025 — Deliverables with version → epic hierarchy

**As** Process Manager,
**I want** to see releases, sprints, and epics in the same view,
**so that** I understand the plan without opening several `.md` files.

## Intent

### Acceptance

- [x] Deliverables tab replaces flat list with sections per version (`v0`, `v1`, …).
- [x] Each version shows outcome, status, sprints, and `done/total` US count.
- [x] Version epics listed with ID, title, status, and US progress.
- [x] Evidence: `DeliverablesView.tsx` integrated in `MonitorDashboard`.

### Why

This story delivered **Deliverables with version → epic hierarchy**: Deliverables tab replaces flat list with sections per version (`v0`, `v1`, …).. It existed so that I understand the plan without opening several `. The record of what shipped lives under `## Technical implementation`.

### Where

Release **v1** (`EPIC-04` in frontmatter — not duplicated here). Depends on US-0012 — read those US files for what must already work. Stops before: Boundaries defined in `docs/epics/EPIC-04.md` and parent version `v1`.

## Plan

### Approach

- Delivered scope recorded under `## Technical implementation`.
- Deliverables tab replaces flat list with sections per version (`v0`, `v1`, …).
- Each version shows outcome, status, sprints, and `done/total` US count.
- Deliver acceptance item: Version epics listed with ID, title, status, and US progress.

### Architecture refs

- docs/05_architecture.md — Monitor, kanban and project loader
- docs/epics/EPIC-04.md — Kanban and user story boundaries
- docs/epics/EPIC-04.md — Epic scope and out-of-scope

### API / DB impact

- _n/a_ — no API or database impact expected.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Planned

- [x] **manual** — open Deliverables; confirm v1 with EPIC-04 and sprints v1-S1/v1-S2.

## Record

### Files

- `app-desktop/src/features/monitor/components/DeliverablesView.tsx` — version → epic hierarchy
- `app-desktop/src/features/monitor/MonitorDashboard.tsx` — Deliverables tab wiring

### Backend

_n/a_

### Frontend

- `DeliverablesView.tsx` — version → epic hierarchy.
- `MonitorDashboard.tsx` — Deliverables tab uses `DeliverablesView`.

### Scripts / Docs

_n/a_

### Executed

- Manual: open Deliverables; confirm v1 with EPIC-04 and sprints v1-S1/v1-S2.

## Boundaries

### Out of scope for this story

- Boundaries defined in `docs/epics/EPIC-04.md` and parent version `v1`.

### Notes

- Completed story — delivery record in `## Technical implementation`. Version: `v1`.
