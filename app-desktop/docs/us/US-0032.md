---
id: US-0032
title: Advanced tools with consistent panel
epic: EPIC-06
version: v1
status: ✅
moscow: Should
depends_on:
  - US-0028
done_when: AdvancedToolsPanel and ScriptValidationPanel use monitorDashedPanelClass and inlineCodeClass.
tests: required
tests_status: done
---

# US-0032 — Advanced tools with consistent panel

**As** Local Operator,
**I want** Python validation in a panel aligned to the monitor design,
**so that** technical tools don't look like a disconnected attachment.

## Acceptance

- [x] Dashed panel with `monitorDashedPanelClass`.
- [x] Inline code with `inlineCodeClass`.
- [x] Validation result in `monitorPanelClass`.
- [x] Evidence: `AdvancedToolsPanel.tsx`, `ScriptValidationPanel.tsx`.

## Context & constraints

### Architecture refs

- docs/05_architecture.md — CLI and script invocation from app
- docs/08_environments.md — Local development commands
- docs/epics/EPIC-06.md — Epic scope and out-of-scope

### API / DB impact

- _n/a_ — no API or database impact expected.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Implementation hints (preliminary)

- Primary files: `src/features/monitor/components/AdvancedToolsPanel.tsx`, `src/features/monitor/components/ScriptValidationPanel.tsx`.
- Dashed panel with `monitorDashedPanelClass`.
- Inline code with `inlineCodeClass`.

## Tests

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

### Planned

- [x] **manual** — expand Advanced tools; run Validate folder in dev.

### Executed

- Manual: expand Advanced tools; run Validate folder in dev.

## Technical implementation

### Files

- `src/features/monitor/components/AdvancedToolsPanel.tsx` — dashed panel wrapper
- `src/features/monitor/components/ScriptValidationPanel.tsx` — validation UI and results

### Backend

- _n/a_

### Frontend

- Advanced tools use `monitorDashedPanelClass`; inline code uses `inlineCodeClass`.
- Validation results render inside `monitorPanelClass` for visual consistency with the monitor.

### Scripts / Docs

- _n/a_

## Out of scope for this story

- Boundaries defined in `docs/epics/EPIC-06.md` and parent version `v1`.

## Notes

- Completed story — delivery record in `## Technical implementation`. Version: `v1`.
