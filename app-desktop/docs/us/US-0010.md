---
id: US-0010
title: Parser de Markdown e frontmatter
epic: EPIC-02
version: v1
status: ✅
moscow: Must
depends_on:
  - US-0009
done_when: Módulo de domínio extrai frontmatter YAML de arquivos .md da pasta
  docs/ sem erros nas US existentes.
tests: required
tests_status: done
---

# US-0010 — Parser de Markdown e frontmatter

**Como** Operador Local,
**quero** que o app leia frontmatter dos `.md` em `docs/`,
**para que** status, depends_on e metadados de US/epics venham dos arquivos, não de mock.

## Aceite

- [x] Parser lê `title`, `status`, `depends_on`, `blocks`, `version`, `updated` dos docs de fase.
- [x] Parser lê frontmatter de `docs/us/US-*.md` (`id`, `epic`, `status`, `moscow`, `depends_on`, `done_when`).
- [x] Parser lê frontmatter de `docs/epics/EPIC-*.md` (`id`, `title`, `status`, `versions`, `outcome`).
- [x] Arquivo inválido gera erro visível na UI, não silencia falha.
- [x] Testes unitários cobrem pelo menos um doc de fase e uma US reais de `app-desktop/docs`.

## Implementação técnica

### Frontend

- `src/domain/meridian/frontmatter.ts`, `parser.ts`, `doc-refs.ts`
- `src/features/folder/project-loader.ts`
- `ParseIssuesBanner` na shell do monitor
- Dependência `yaml` para frontmatter multilinha

## Testes

> Na **criação**: preencher **Planejado**. Ao **fechar** (`complete-user-story`): marcar `[x]` e registrar em **Executado**.

> Na **criação**: preencher **Planejado**. Ao **fechar** (`complete-user-story`): marcar `[x]` e registrar em **Executado**.

### Planejado

- [x] **automated** — `pnpm test` — `parser.test.ts` com fixtures de `docs/00_scope.md`, `05_architecture.md`, `us/US-0001.md`, `epics/EPIC-02.md`
- [x] **build** — `pnpm build` passou

### Executado

- `pnpm test` — `parser.test.ts` com fixtures de `docs/00_scope.md`, `05_architecture.md`, `us/US-0001.md`, `epics/EPIC-02.md`
- `pnpm build` passou

## Fora de escopo desta story

- Montar as três abas com dados parseados (US-0011–US-0013).
- Rodar `validate_meridian.py`.

## Notas

- US-0011 conecta o parser à aba Configuração inicial.
