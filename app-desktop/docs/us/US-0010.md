---
id: US-0010
title: Markdown and frontmatter parser
epic: EPIC-02
version: v1
status: ✅
moscow: Must
depends_on:
done_when: Domain module extracts YAML frontmatter from .md files in docs/ without errors on existing US.
tests: required
tests_status: done
ready: true
---

# US-0010 — Markdown and frontmatter parser

**As** Local Operator,
**I want** the app to read frontmatter from `.md` files in `docs/`,
**so that** status, depends_on, and metadata from US/epics come from the files, not from mock data.

## Intent

### Acceptance

- [x] Parser reads `title`, `status`, `depends_on`, `blocks`, `version`, `updated` from phase docs.
- [x] Parser reads frontmatter from `docs/us/US-*.md` (`id`, `epic`, `status`, `moscow`, `depends_on`, `done_when`).
- [x] Parser reads frontmatter from `docs/epics/EPIC-*.md` (`id`, `title`, `status`, `versions`, `outcome`).
- [x] An invalid file generates a visible error in the UI and does not silently fail.
- [x] Unit tests cover at least one real phase doc and one real US from `app-desktop/docs`.

### Why

This story delivered **Markdown and frontmatter parser**: Parser reads `title`, `status`, `depends_on`, `blocks`, `version`, `updated` from phase docs.. It existed so that status, depends_on, and metadata from US/epics come from the files, not from mock data. The record of what shipped lives under `## Technical implementation`.

### Where

Release **v1** (`EPIC-02` in frontmatter — not duplicated here). Depends on US-0009 — read those US files for what must already work. Stops before: Populate the three tabs with parsed data (US-0011–US-0013).

## Plan

### Approach

- Primary files: `src/domain/meridian/frontmatter.ts`, `src/features/folder/project-loader.ts`.
- Parser reads `title`, `status`, `depends_on`, `blocks`, `version`, `updated` from phase docs.
- Parser reads frontmatter from `docs/us/US-*.md` (`id`, `epic`, `status`, `moscow`, `depends_on`, `done_when`).

### Architecture refs

- docs/05_architecture.md — Project folder access and docs/ layout
- docs/04_principles.md — Docs-first and local-first operation
- docs/epics/EPIC-02.md — Epic scope and out-of-scope

### API / DB impact

- Documentation-only — no API or database changes.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Planned

- [x] **automated** — `pnpm test` — `parser.test.ts` with fixtures from `docs/00_scope.md`, `05_architecture.md`, `us/US-0001.md`, `epics/EPIC-02.md`
- [x] **build** — `pnpm build` passed

## Record

### Files

_(fill on close)_

### Backend

_(fill on close or \_n/a_)\_

### Frontend

- `src/domain/meridian/frontmatter.ts`, `parser.ts`, `doc-refs.ts`
- `src/features/folder/project-loader.ts`
- `ParseIssuesBanner` in the monitor shell
- `yaml` dependency for multiline frontmatter

### Scripts / Docs

_(fill on close or \_n/a_)\_

### Executed

- `pnpm test` — `parser.test.ts` with fixtures from `docs/00_scope.md`, `05_architecture.md`, `us/US-0001.md`, `epics/EPIC-02.md`
- `pnpm build` passed

## Boundaries

### Out of scope for this story

- Populate the three tabs with parsed data (US-0011–US-0013).
- Run `validate_meridian.py`.

### Notes

- US-0011 connects the parser to the Initial setup tab.
