---
id: US-0039
title: Log de decisões JSON por dia
epic: EPIC-03
version: v1
status: ✅
moscow: Must
depends_on:
  - US-0014
done_when: docs/decisions/YYYY-MM-DD.json é fonte do log; validate_meridian.py
  valida schema; kit .agent documenta prepend em entries.
tests: required
tests_status: done
---

# US-0039 — Log de decisões JSON por dia

**Como** Manager do Processo,
**quero** decisões em JSON estruturado por dia calendário,
**para que** agentes, validador e app tratem o log como dado auditável (não markdown).

## Aceite

- [x] Pasta `docs/decisions/` com arquivos `YYYY-MM-DD.json` (`date` + array `entries`).
- [x] Cada entrada exige `time`, `title`, `affected_document`, `what_changed`, `why_changed`, `impact`, `responsible`.
- [x] `11_decisions.md` é stub com regras (não acumula entradas).
- [x] `validate_meridian.py` valida pasta, nome, JSON e campos obrigatórios.
- [x] Skill `update-decisions-log` e §11.12 de `.agent/MERIDIAN.md` descrevem prepend em `entries`.
- [x] Migração dogfooding: `app-desktop/docs/decisions/2026-06-02.json`.

## Testes

### Planejado

- [x] **automated** — `python3 .agent/scripts/validate_meridian.py app-desktop` passa.
- [x] **automated** — parser test `decisions/2026-06-02.json` em `parser.test.ts`.

### Executado

- `validate_meridian.py app-desktop` — passed.
- `pnpm test` — parser de decisions incluído.

## Implementação técnica

| Camada    | Arquivos                                                                |
| --------- | ----------------------------------------------------------------------- |
| Dados     | `app-desktop/docs/decisions/2026-06-02.json`, stub `11_decisions.md`    |
| Parser    | `parser.ts` — `parseDecisionDayFile` (JSON)                             |
| Loader    | `project-loader.ts` — `loadDecisions`                                   |
| Validador | `.agent/scripts/validate_meridian.py`                                   |
| Protocolo | `.agent/MERIDIAN.md` §11.12, skill `update-decisions-log`, init-project |
