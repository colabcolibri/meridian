---
id: US-0039
title: JSON decision log per day
epic: EPIC-03
version: v1
status: ✅
moscow: Must
depends_on:
  - US-0014
done_when: docs/decisions/YYYY-MM-DD.json is log source; validate_meridian.py validates schema; .agent kit documents prepend in entries.
tests: required
tests_status: done
---

# US-0039 — JSON decision log per day

**As** Process Manager,
**I want** decisions in structured JSON by calendar day,
**so that** agents, validator, and app treat the log as auditable data (not markdown).

## Acceptance

- [x] Folder `docs/decisions/` with `YYYY-MM-DD.json` files (`date` + `entries` array).
- [x] Each entry requires `time`, `title`, `affected_document`, `what_changed`, `why_changed`, `impact`, `responsible`.
- [x] `11_decisions.md` is stub with rules (does not accumulate entries).
- [x] `validate_meridian.py` validates folder, name, JSON, and required fields.
- [x] Skill `update-decisions-log` and §11.12 of `.agent/MERIDIAN.md` describe prepend in `entries`.
- [x] Dogfooding migration: `app-desktop/docs/decisions/2026-06-02.json`.

## Tests

### Planned

- [x] **automated** — `python3 .agent/scripts/validate_meridian.py app-desktop` passes.
- [x] **automated** — parser test `decisions/2026-06-02.json` in `parser.test.ts`.

### Executed

- `validate_meridian.py app-desktop` — passed.
- `pnpm test` — decisions parser included.

## Technical implementation

| Layer     | Files                                                                   |
| --------- | ----------------------------------------------------------------------- |
| Data      | `app-desktop/docs/decisions/2026-06-02.json`, stub `11_decisions.md`    |
| Parser    | `parser.ts` — `parseDecisionDayFile` (JSON)                             |
| Loader    | `project-loader.ts` — `loadDecisions`                                   |
| Validator | `.agent/scripts/validate_meridian.py`                                   |
| Protocol  | `.agent/MERIDIAN.md` §11.12, skill `update-decisions-log`, init-project |
