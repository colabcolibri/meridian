---
id: US-0114
title: Repo lineage — meridian-v1-old and main cutover
epic: EPIC-14
version: v9
status: ❌
moscow: Must
depends_on: [US-0112]
ready: true
done_when: "Branch meridian-v1-old exists; MERIDIAN_V2_CUTOVER.md runbook reviewed; README notes v1 vs v2."
tests: none
tests_status: n/a
---

# US-0114 — Repo lineage — meridian-v1-old and main cutover

**As** Process Manager,
**I want** the current Meridian repository preserved as meridian-v1-old while main evolves as Meridian 2.0,
**so that** we can reference the Markdown-based protocol and roll back if needed without losing history.

## Intent

### Acceptance

- [ ] Runbook `MERIDIAN_V2_CUTOVER.md` at repo root with ordered steps: pre-flight checklist, create branch, migrate dogfood, validate, monitor smoke, announce, post-cutover kit behavior.
- [ ] Git branch `meridian-v1-old` created from commit immediately before cutover merge (document exact command; execution is manager-gated).
- [ ] Optional tag `meridian-v1-final` documented as alternative to branch.
- [ ] Root `README.md` section: v1 lineage on `meridian-v1-old`; v2 (SQLite) on `main`.
- [ ] Checklist items: migration script run, `validate_meridian.py` pass, monitor smoke, no force-push to main without approval.
- [ ] Decision logged in `docs/decisions/` on execution day.

### Why

The user asked to make this repo meridian-v1-old and have the new Meridian on main. That is a repository governance action, not runtime code — but it must be documented and executed in order so contributors know which branch to clone for legacy kit vs Meridian 2.0.

### Where

**v9-S4**, final slice of **v9 / EPIC-14**. Depends on **US-0112** (product works on DB before cutover announcement). Runbook can be drafted early; branch creation executes at v9 close.

## Plan

### Approach

- Author `MERIDIAN_V2_CUTOVER.md` with copy-paste git commands: `git branch meridian-v1-old <sha>`, `git push -u origin meridian-v1-old`, and rollback procedure (`git checkout meridian-v1-old`).
- Add README subsection "Repository lineage" — three sentences + link to runbook; no marketing prose.
- Pre-flight section references exact commands from US-0106, US-0107, US-0109, US-0112 manual steps — DRY, no duplicate implementation detail.

### Architecture refs

- `docs/05_architecture.md` — § Repository context
- `docs/05_architecture.md` — § Objective

### API / DB impact

- _n/a_

### Security notes

- Runbook warns against force-push to `main` and documents manager approval gate.

### Related decisions

- Entry on execution: "Meridian v2 cutover — meridian-v1-old branch created".

### Planned

- [ ] **manual** — Manager reviews `MERIDIAN_V2_CUTOVER.md` and approves timing.
- [ ] **manual** — After approval: `git branch -a | grep meridian-v1-old` shows branch on remote.
- [ ] **manual** — README lineage section visible on GitHub default view.

## Record

### Files

_(fill on close)_

### Backend

_n/a_

### Frontend

_n/a_

### Scripts / Docs

_(fill on close)_

### Executed

_(pending until close)_

## Boundaries

### Out of scope for this story

- Creating SQLite schema or migration (prior US).
- Separate GitHub repository — same repo, branch/tag only.
- Automated branch creation in CI — manual manager action.
- Archiving npm/VSIX under old version scheme.

### Notes

- US delivers runbook + README; branch push requires explicit manager command in chat.
