---
id: US-0003
title: Configure Git and local quality
epic: EPIC-01
version: v0
status: ✅
moscow: Must
depends_on:
done_when: Git, gitignore, Prettier, Husky, and lint-staged are configured.
tests: required
tests_status: done
ready: true
---

# US-0003 — Configure Git and local quality

**As** Process Manager,
**I want** the project to have Git, protected sensitive files, and local checks,
**so that** AI agents and people work with consistency from the foundation.

## Intent

### Acceptance

- [ ] Git repository initialized.
- [ ] `.gitignore` protects `.env`, builds, dependencies, logs, and incorrect lockfiles.
- [ ] `.env.example` exists as configuration contract.
- [ ] Prettier configured.
- [ ] Husky configured.
- [ ] lint-staged configured in pre-commit.
- [ ] `pnpm format:check`, `pnpm lint`, and `pnpm build` pass.

### Why

This story delivered **Configure Git and local quality**: Git repository initialized.. It existed so that AI agents and people work with consistency from the foundation. The record of what shipped lives under `## Technical implementation`.

### Where

Release **v0** (`EPIC-01` in frontmatter — not duplicated here). Depends on US-0001 — read those US files for what must already work. Stops before: Initial commit.

## Plan

### Approach

- Primary files: `.husky/pre-commit`.
- Git repository initialized.
- `.gitignore` protects `.env`, builds, dependencies, logs, and incorrect lockfiles.
- Deliver acceptance item: `.env.example` exists as configuration contract.

### Architecture refs

- docs/05_architecture.md — Monorepo layout and app-desktop shell
- docs/01_tech_stack.md — Vite, React, TypeScript, Tailwind
- docs/epics/EPIC-01.md — Epic scope and out-of-scope

### API / DB impact

- _n/a_ — no API or database impact expected.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Planned

- [x] **lint** — `pnpm format:check`
- [x] **lint** — `pnpm lint`
- [x] **build** — `pnpm build`

## Record

### Files

_(fill on close)_

### Backend

_(fill on close or \_n/a_)\_

### Frontend

- Add quality scripts to `package.json`.
- Add `.editorconfig`, `.prettierrc.json`, `.prettierignore`, and VS Code settings.
- Configure `.husky/pre-commit` hook.

### Scripts / Docs

_(fill on close or \_n/a_)\_

### Executed

- `pnpm format:check`, `pnpm lint`, and `pnpm build` passed in `app-desktop/`.

## Boundaries

### Out of scope for this story

- Initial commit.
- Remote CI pipeline.
- Commit message rules.

### Notes

- The project uses `pnpm-lock.yaml` as the sole lockfile.
