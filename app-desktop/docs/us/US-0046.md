---
id: US-0046
title: Meridian Validate Project command
epic: EPIC-05
version: v4
status: ❌
moscow: Must
depends_on:
done_when: Command Meridian Validate Project runs validate_meridian.py and lists errors and warnings in the editor.
tests: required
tests_status: pending
ready: true
---

# US-0046 — Meridian Validate Project command

**As** Future VSCode User,
**I want** to validate my Meridian folder from the editor,
**so that** I catch protocol issues before committing.

## Intent

### Acceptance

- [ ] Command palette: **Meridian: Validate Project** (`meridian.validateProject`).
- [ ] Runs `python3 .agent/scripts/validate_meridian.py <project-root>`.
- [ ] Output channel "Meridian" lists errors and warnings with file paths.
- [ ] Status bar or notification summary: pass / N errors / M warnings.
- [ ] Works on `app-desktop/` dogfood root (project with `docs/` subfolder).

### Why

This story adds **Meridian Validate Project command** — to validate my Meridian folder from the editor. When done, I catch protocol issues before committing. It is intentionally one slice; it does not deliver the whole epic alone.

### Where

Release **v2** (`EPIC-05` in frontmatter — not duplicated here). Depends on US-0044 — read those US files for what must already work. Stops before: Problems panel diagnostics integration (optional follow-up).

## Plan

### Approach

- Scope: Meridian Validate Project command under `v2`.
- Command palette: **Meridian: Validate Project** (`meridian.validateProject`).
- Runs `python3 .agent/scripts/validate_meridian.py <project-root>`.
- Deliver acceptance item: Output channel "Meridian" lists errors and warnings with file paths.

### Architecture refs

- docs/05_architecture.md — Editor integration and disk writes
- docs/02_security.md — Local file access and agent boundaries
- docs/epics/EPIC-05.md — Epic scope and out-of-scope

### API / DB impact

- Documentation-only — no API or database changes.

### Security notes

- Follow `docs/02_security.md` — local file access, secrets, and agent write boundaries.

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Planned

- [ ] **manual** — Valid project → "validation passed" (warnings ok).
- [ ] **manual** — Remove required folder → errors listed with path.

## Record

### Files

_(fill in when implementation is complete)_

### Backend

_(fill in when applicable)_

### Frontend

_(fill in when applicable)_

### Scripts / Docs

_(fill in when applicable)_

### Executed

_(pending)_

## Boundaries

### Out of scope for this story

- Problems panel diagnostics integration (optional follow-up).
- Replacing dev-server validate bridge in `app-desktop` monitor.

### Notes

- Requires Python 3 on PATH; document in 08_environments if missing.
