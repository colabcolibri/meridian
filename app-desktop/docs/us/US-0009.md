---
id: US-0009
title: Abrir pasta Meridian no app
epic: EPIC-02
version: v1
status: ✅
moscow: Must
depends_on:
  - US-0008
done_when: Usuário escolhe a pasta docs/ do projeto e o app persiste o handle da
  sessão para releitura.
tests: required
tests_status: done
---

# US-0009 — Abrir pasta Meridian no app

**Como** Manager do Processo,
**quero** abrir a pasta `docs/` do meu projeto Meridian no app desktop,
**para que** o monitor mostre o mesmo conteúdo que edito com agentes no Cursor.

## Aceite

- [x] Botão **Abrir pasta** habilitado na shell do monitor.
- [x] Pasta válida é a pasta `docs/` do projeto (`00_scope.md` na raiz e subpasta `us/`).
- [x] Pasta inválida exibe mensagem clara sem quebrar o app.
- [x] Pasta escolhida é reaberta na mesma sessão do browser (File System Access API + IndexedDB).
- [x] Dogfooding: abrir `app-desktop/docs/` como pasta monitorada funciona.

## Implementação técnica

### Frontend

- `src/features/folder/` — `folder-access`, `validate-meridian-folder`, `folder-handle-store`, `ProjectFolderContext`.
- `FolderToolbar` e `OpenFolderPrompt` no monitor.

## Testes

> Na **criação**: preencher **Planejado**. Ao **fechar** (`complete-user-story`): marcar `[x]` e registrar em **Executado**.

### Planejado

- [x] **build** — `pnpm build` passou.
- [x] **manual** — `pnpm dev`, Abrir pasta docs → `app-desktop/docs/`, recarregar e confirmar handle.

### Executado

- `pnpm build` passou.
- Manual: File System Access + IndexedDB restauram a pasta `app-desktop/docs/` na sessão.

## Fora de escopo desta story

- Parser de conteúdo (US-0010).
- Escrita em arquivos.

## Notas

- Próxima: US-0010 (parser) para alimentar as três abas com os `.md` da pasta aberta.
