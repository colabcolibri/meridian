---
id: US-0009
title: Open Meridian folder in the app
epic: EPIC-02
version: v1
status: ✅
moscow: Must
depends_on:
  - US-0008
done_when: User selects the project's docs/ folder and the app persists the session handle for re-reading.
tests: required
tests_status: done
---

# US-0009 — Open Meridian folder in the app

**As** Process Manager,
**I want** to open the `docs/` folder of my Meridian project in the desktop app,
**so that** the monitor shows the same content I edit with agents in Cursor.

## Acceptance

- [x] **Open folder** button enabled in the monitor shell.
- [x] A valid folder is the project's `docs/` folder (`00_scope.md` at root with `us/` subfolder).
- [x] An invalid folder shows a clear message without breaking the app.
- [x] The chosen folder is reopened in the same browser session (File System Access API + IndexedDB).
- [x] Dogfooding: opening `app-desktop/docs/` as the monitored folder works.

## Context & constraints

### Architecture refs

- docs/05_architecture.md — Project folder access and docs/ layout
- docs/04_principles.md — Docs-first and local-first operation
- docs/epics/EPIC-02.md — Epic scope and out-of-scope

### API / DB impact

- Documentation-only — no API or database changes.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Implementation hints (preliminary)

- Primary files: `src/features/folder/`.
- **Open folder** button enabled in the monitor shell.
- A valid folder is the project's `docs/` folder (`00_scope.md` at root with `us/` subfolder).

## Technical implementation

### Frontend

- `src/features/folder/` — `folder-access`, `validate-meridian-folder`, `folder-handle-store`, `ProjectFolderContext`.
- `FolderToolbar` and `OpenFolderPrompt` in the monitor.

## Tests

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

### Planned

- [x] **build** — `pnpm build` passed.
- [x] **manual** — `pnpm dev`, open docs folder → `app-desktop/docs/`, reload and confirm handle.

### Executed

- `pnpm build` passed.
- Manual: File System Access + IndexedDB restore `app-desktop/docs/` folder in session.

## Out of scope for this story

- Content parser (US-0010).
- File writes.

## Notes

- Next: US-0010 (parser) to feed the three tabs with `.md` files from the opened folder.
