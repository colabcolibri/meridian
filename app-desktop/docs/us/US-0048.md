---
id: US-0048
title: Validate US frontmatter before write
epic: EPIC-05
version: v2
status: ❌
moscow: Must
depends_on:
  - US-0047
done_when: New US command rejects invalid epic, version, depends_on, or empty done_when before writing file.
tests: required
tests_status: pending
ready: true
---

# US-0048 — Validate US frontmatter before write

**As** Future VSCode User,
**I want** validation before a US file is created,
**so that** I do not save invalid stories that break the board or validator.

## Acceptance

- [ ] Before write in US-0047 flow, validate epic id exists in `docs/epics/`.
- [ ] Validate version id exists in `docs/versions/`.
- [ ] Validate `depends_on` entries reference existing US ids (if provided).
- [ ] Reject empty `done_when` and duplicate US id.
- [ ] Show error message with fix hint; no partial file on disk.
- [ ] Same rules align with `validate_meridian.py` US checks where applicable.

## Context & constraints

### Why this story

This story adds **Validate US frontmatter before write** — validation before a US file is created. When done, I do not save invalid stories that break the board or validator. It is intentionally one slice; it does not deliver the whole epic alone.

### Where it fits

Release **v2** (`EPIC-05` in frontmatter — not duplicated here). Depends on US-0047 — read those US files for what must already work. Stops before: Validating full acceptance checklist on every save.

### Approach

- Scope: Validate US frontmatter before write under `v2`.
- Before write in US-0047 flow, validate epic id exists in `docs/epics/`.
- Validate version id exists in `docs/versions/`.
- Deliver acceptance item: Validate `depends_on` entries reference existing US ids (if provided).

### Architecture refs

- docs/05_architecture.md — Editor integration and disk writes
- docs/02_security.md — Local file access and agent boundaries
- docs/epics/EPIC-05.md — Epic scope and out-of-scope

### API / DB impact

- Documentation-only — no API or database changes.

### Security notes

- Follow `docs/02_security.md` — local file access, secrets, and agent write boundaries.

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

## Technical implementation

> On **creation**: placeholder below. On **completion** (`✅`): skill `complete-user-story` — replace with real record (files + layers).

### Files

_(fill in when implementation is complete)_

### Backend

_(fill in when applicable)_

### Frontend

_(fill in when applicable)_

### Scripts / Docs

_(fill in when applicable)_

## Tests

### Planned

- [ ] **manual** — Pick non-existent epic → blocked with message.
- [ ] **manual** — Valid picks → file written.

### Executed

_(pending)_

## Out of scope for this story

- Validating full acceptance checklist on every save.
- Linting markdown body prose.

## Notes

- Can share validation helpers with generate_board.py where practical.
