---
id: US-0031
title: Setup with consistent visual tokens
epic: EPIC-06
version: v1
status: ✅
moscow: Must
depends_on:
  - US-0028
done_when: Locked/draft states and phase icon use bg-muted and stroke-border; no zinc-* on Setup tab.
tests: required
tests_status: done
---

# US-0031 — Setup with consistent visual tokens

**As** Process Manager,
**I want** the Setup tab with the same visual language as the other tabs,
**so that** I read phase doc progress without loose colors.

## Acceptance

- [x] `setup-step-styles.ts`: locked and draft use `bg-muted` / `text-muted-foreground`.
- [x] `PhaseProgressIcon`: empty ring uses `stroke-border`.
- [x] Evidence: grep with no `zinc-*` in setup components.

## Context & constraints

### Architecture refs

- docs/05_architecture.md — CLI and script invocation from app
- docs/08_environments.md — Local development commands
- docs/epics/EPIC-06.md — Epic scope and out-of-scope

### API / DB impact

- _n/a_ — no API or database impact expected.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Implementation hints (preliminary)

- Primary files: `src/features/monitor/setup-step-styles.ts`, `src/features/monitor/components/PhaseProgressIcon.tsx`.
- `setup-step-styles.ts`: locked and draft use `bg-muted` / `text-muted-foreground`.
- `PhaseProgressIcon`: empty ring uses `stroke-border`.

## Tests

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

### Planned

- [x] **manual** — open Setup — locked tiles consistently grey matching the rest of the app.

### Executed

- Manual: open Setup — locked tiles consistently grey matching the rest of the app.

## Technical implementation

### Files

- `src/features/monitor/setup-step-styles.ts` — locked/draft states with muted tokens
- `src/features/monitor/components/PhaseProgressIcon.tsx` — empty ring uses `stroke-border`

### Backend

- _n/a_

### Frontend

- Locked and draft setup steps use `bg-muted` / `text-muted-foreground`.
- Phase progress icon ring uses design-system border token; no loose `zinc-*` on Setup tab.

### Scripts / Docs

- _n/a_

## Out of scope for this story

- Boundaries defined in `docs/epics/EPIC-06.md` and parent version `v1`.

## Notes

- Completed story — delivery record in `## Technical implementation`. Version: `v1`.
