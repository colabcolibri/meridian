---
id: US-0037
title: Compact board with details in sheet
epic: EPIC-04
version: v1
status: ✅
moscow: Must
depends_on:
  - US-0035
done_when: Kanban cards are compact rows; done_when and deps open in sheet; empty columns hidden when filtering version.
tests: required
tests_status: done
---

# US-0037 — Compact board with details in sheet

**As** Process Manager,
**I want** lean cards on the board with on-demand details,
**so that** the screen doesn't freeze or truncate everything in small boxes.

## Acceptance

- [x] Card shows ID, title, and epic in compact layout (no inline `<details>`).
- [x] Click opens side sheet with done_when and dependencies.
- [x] With version filtered, columns without US are hidden.
- [x] Evidence: `KanbanView.tsx`, `StoryDetailSheet.tsx`.

## Context & constraints

### Why this story

This story delivered **Compact board with details in sheet**: Card shows ID, title, and epic in compact layout (no inline `<details>`).. It existed so that the screen doesn't freeze or truncate everything in small boxes. The record of what shipped lives under `## Technical implementation`.

### Where it fits

Release **v1** (`EPIC-04` in frontmatter — not duplicated here). Depends on US-0035 — read those US files for what must already work. Stops before: Boundaries defined in `docs/epics/EPIC-04.md` and parent version `v1`.

### Approach

- Primary files: `src/features/monitor/components/KanbanView.tsx`, `src/features/monitor/components/StoryDetailSheet.tsx`.
- Card shows ID, title, and epic in compact layout (no inline `<details>`).
- Click opens side sheet with done_when and dependencies.
- Deliver acceptance item: With version filtered, columns without US are hidden.

### Architecture refs

- docs/05_architecture.md — Monitor, kanban and project loader
- docs/epics/EPIC-04.md — Kanban and user story boundaries
- docs/epics/EPIC-04.md — Epic scope and out-of-scope

### API / DB impact

- _n/a_ — no API or database impact expected.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

## Tests

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

### Planned

- [x] **manual** — filter v1, click US — sheet with done_when; empty columns disappear.

### Executed

- Manual: filter v1, click US — sheet with done_when; empty columns disappear.

## Technical implementation

### Files

- `src/features/monitor/components/KanbanView.tsx` — compact card rows, hide empty columns when filtered
- `src/features/monitor/components/StoryDetailSheet.tsx` — side sheet with `done_when` and dependencies

### Backend

- _n/a_

### Frontend

- Cards show ID, title, and epic in compact layout (no inline `<details>`).
- Click opens sheet with full story details; columns without US for selected version are hidden.

### Scripts / Docs

- _n/a_

## Out of scope for this story

- Boundaries defined in `docs/epics/EPIC-04.md` and parent version `v1`.

## Notes

- Completed story — delivery record in `## Technical implementation`. Version: `v1`.
