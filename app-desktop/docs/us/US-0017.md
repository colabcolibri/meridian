---
id: US-0017
title: Ler documentos de fase em Markdown no app
epic: EPIC-02
version: v1
status: ✅
moscow: Must
depends_on: [US-0009]
done_when: "Na aba Configuração inicial, cada doc 00–11 abre o .md correspondente na raiz da pasta docs aberta."
---

# US-0017 — Ler documentos de fase em Markdown no app

**Como** Manager do Processo,
**quero** abrir e ler cada arquivo `docs/00–11/*.md` dentro do monitor,
**para que** eu revise escopo, versões e arquitetura sem sair do app nem duplicar contexto para o agente.

## Aceite

- [x] Cada etapa da Configuração inicial tem ação **Ler .md**.
- [x] Com pasta aberta, o app lê `docs/{id}.md` via File System Access API.
- [x] Painel exibe frontmatter (YAML) e corpo do documento.
- [x] Erro claro se pasta não estiver aberta ou arquivo não existir.
- [x] Funciona para os 12 docs (`00_scope` … `11_decisions`) ao abrir pasta `app-desktop/docs/`.
- [x] `pnpm build` passa.

## Implementação técnica

### Frontend

- `read-phase-doc.ts` — leitura e separação de frontmatter.
- `PhaseDocReader.tsx` — painel de leitura na aba Configuração inicial.
- Botão **Ler .md** em cada linha de `SetupMonitorView`.

## Testes

- `pnpm build` passou.
- Manual: abrir pasta `app-desktop/docs/`, Configuração inicial → Ler .md em `06_versions.md` e `07_architecture.md`.

## Fora de escopo desta story

- Editar e salvar Markdown (v2 / extensão).
- Parser completo reutilizado em todas as abas (consolidar em US-0010).
- Renderização markdown avançada (tabelas, código com highlight).

## Notas

- Epic: **EPIC-02** (capacidade de produto, não epic novo).
- US-0011 continua responsável por lista/status vindos do parser; US-0017 é **leitura** do arquivo.
