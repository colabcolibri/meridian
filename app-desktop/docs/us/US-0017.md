---
id: US-0017
title: Read phase documents in Markdown in the app
epic: EPIC-02
version: v1
status: ✅
moscow: Must
depends_on:
  - US-0009
done_when: On Setup tab, each phase document (00–08 and 11) opens the corresponding .md at root of opened docs folder.
tests: required
tests_status: done
---

# US-0017 — Read phase documents in Markdown in the app

**As** Process Manager,
**I want** to open and read each phase document (`00–08` and `11`) inside the monitor,
**so that** I can review scope, versions, and architecture without leaving the app or duplicating context for the agent.

## Acceptance

- [x] Each step in Initial setup has a **Read .md** action.
- [x] With folder open, app reads `docs/{id}.md` via File System Access API.
- [x] Panel displays frontmatter (YAML) and document body.
- [x] Clear error if folder is not open or file does not exist.
- [x] Works for all 10 phase docs (`00_scope` … `08_environments`, `11_decisions`) when opening `app-desktop/docs/`.
- [x] `pnpm build` passes.

## Context & constraints

### Architecture refs

- docs/05_architecture.md — Project folder access and docs/ layout
- docs/04_principles.md — Docs-first and local-first operation
- docs/epics/EPIC-02.md — Epic scope and out-of-scope

### API / DB impact

- Documentation-only — no API or database changes.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Implementation hints (preliminary)

- Delivered scope recorded under `## Technical implementation`.
- Each step in Initial setup has a **Read .md** action.
- With folder open, app reads `docs/{id}.md` via File System Access API.

## Technical implementation

### Frontend

- `read-phase-doc.ts` — reading and frontmatter separation.
- `PhaseDocReader.tsx` — reading panel in the Initial setup tab.
- **Read .md** button on each row of `SetupMonitorView`.

## Tests

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

### Planned

- [x] **build** — `pnpm build` passed.
- [x] **manual** — open `app-desktop/docs/` folder, Setup → Read .md on `05_architecture.md` and `04_principles.md`.

### Executed

- `pnpm build` passed.
- Manual: open `app-desktop/docs/` folder, Setup → Read .md on `05_architecture.md` and `04_principles.md`.

## Out of scope for this story

- Edit and save Markdown (v2 / extension).
- Full parser reused across all tabs (consolidate in US-0010).
- Advanced markdown rendering (tables, code with highlight).

## Notes

- Epic: **EPIC-02** (product capability, not a new epic).
- US-0011 remains responsible for list/status from parser; US-0017 is **reading** the file.
