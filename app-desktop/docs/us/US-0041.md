---
id: US-0041
title: Scaffold app-visual-studio extension
epic: EPIC-05
version: v2
status: ❌
moscow: Must
depends_on:
done_when: app-visual-studio/ loads in Extension Development Host with pnpm dev/build scripts documented.
tests: required
tests_status: pending
ready: true
---

# US-0041 — Scaffold app-visual-studio extension

**As** Future VSCode User,
**I want** a runnable VS Code extension project in the monorepo,
**so that** I can develop Meridian disk writes inside the editor.

## Intent

### Acceptance

- [ ] Folder `app-visual-studio/` at repository root with `package.json`, `tsconfig.json`, and `src/extension.ts`.
- [ ] Extension manifest: publisher placeholder, `activationEvents` on workspace containing `.agent/MERIDIAN.md`.
- [ ] Commands registered (stubs ok): `meridian.syncBoard`, `meridian.validateProject`, `meridian.newUserStory`.
- [ ] `pnpm install`, `pnpm compile`, and F5 (Extension Development Host) documented in `app-visual-studio/README.md`.
- [ ] Root `README.md` roadmap mentions `app-visual-studio/` as v2 deliverable.

### Why

This story adds **Scaffold app-visual-studio extension** — a runnable VS Code extension project in the monorepo. When done, I can develop Meridian disk writes inside the editor. It is intentionally one slice; it does not deliver the whole epic alone.

### Where

Release **v2** (`EPIC-05` in frontmatter — not duplicated here). Depends on US-0040 — read those US files for what must already work. Stops before: Real command implementations (v2-S2/S3).

## Plan

### Approach

- Scope: Scaffold app-visual-studio extension under `v2`.
- Folder `app-visual-studio/` at repository root with `package.json`, `tsconfig.json`, and `src/extension.ts`.
- Extension manifest: publisher placeholder, `activationEvents` on workspace containing `.agent/MERIDIAN.md`.
- Deliver acceptance item: Commands registered (stubs ok): `meridian.syncBoard`, `meridian.validateProject`, `meridian.newUserStory`.

### Architecture refs

- docs/05_architecture.md — Editor integration and disk writes
- docs/02_security.md — Local file access and agent boundaries
- docs/epics/EPIC-05.md — Epic scope and out-of-scope

### API / DB impact

- _n/a_ — no API or database impact expected.

### Security notes

- Follow `docs/02_security.md` — local file access, secrets, and agent write boundaries.

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Planned

- [ ] **manual** — F5 in VS Code → extension activates without errors in Output panel.
- [ ] **manual** — `pnpm compile` in `app-visual-studio/` exits 0.

## Record

### Files

_(fill in when implementation is complete)_

### Backend

_(fill in when applicable)_

### Frontend

_(fill in when applicable)_

### Scripts / Docs

_(fill in when applicable)_

### Executed

_(pending)_

## Boundaries

### Out of scope for this story

- Real command implementations (v2-S2/S3).
- Publishing to VS Code Marketplace.
- Shared npm package with `app-desktop`.

### Notes

- Follow VS Code Extension API + esbuild or `@vscode/test-electron` patterns from `01_tech_stack.md` (updated in US-0043).
