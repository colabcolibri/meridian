---
id: US-0026
title: Board sorts US by ID
epic: EPIC-04
version: v1
status: ✅
moscow: Should
depends_on:
  - US-0022
done_when: Kanban columns list US in ascending numeric order (US-0001, US-0002…).
tests: required
tests_status: done
---

# US-0026 — Board sorts US by ID

**As** Process Manager,
**I want** cards sorted by US ID,
**so that** I find a story without hunting visually.

## Acceptance

- [x] Within each column, US sorted by ID (numeric comparison).
- [x] Order maintained after applying epic or version filters.
- [x] Evidence: `sortStoriesById()` in `validators.ts` used by `groupStoriesByStatus()`.

## Context & constraints

### Architecture refs

- docs/05_architecture.md — Monitor, kanban and project loader
- docs/epics/EPIC-04.md — Kanban and user story boundaries
- docs/epics/EPIC-04.md — Epic scope and out-of-scope

### API / DB impact

- _n/a_ — no API or database impact expected.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Implementation hints (preliminary)

- Delivered scope recorded under `## Technical implementation`.
- Within each column, US sorted by ID (numeric comparison).
- Order maintained after applying epic or version filters.

## Technical implementation

### Domain

- `validators.ts` — `sortStoriesById` + usage in `groupStoriesByStatus`.

## Tests

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

### Planned

- [x] **manual** — Pending column — IDs ascending from top to bottom.

### Executed

- Manual: Pending column — IDs ascending from top to bottom.

## Out of scope for this story

- Boundaries defined in `docs/epics/EPIC-04.md` and parent version `v1`.

## Notes

- Completed story — delivery record in `## Technical implementation`. Version: `v1`.
