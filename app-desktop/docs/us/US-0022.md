---
id: US-0022
title: Kanban with lean cards
epic: EPIC-06
version: v1
status: ✅
moscow: Should
depends_on:
  - US-0021
done_when: Cards show title and delivery; technical details collapsed; epic filter shows readable name.
tests: required
tests_status: done
---

# US-0022 — Kanban with lean cards

**As** Process Manager,
**I want** to read the deliverables board quickly,
**so that** I prioritize without infinite scrolling per card.

## Acceptance

- [x] Card: ID, title, epic name (not just code), MoSCoW.
- [x] `done_when` and dependencies in `<details>` per card.
- [x] Epic filters with abbreviated title.
- [x] Columns with minimum height and horizontal scroll on mobile.
- [x] Evidence: `KanbanView.tsx` with `<details>` and filters by epic title.

## Context & constraints

### Why this story

This story delivered **Kanban with lean cards**: Card: ID, title, epic name (not just code), MoSCoW.. It existed so that I prioritize without infinite scrolling per card. The record of what shipped lives under `## Technical implementation`.

### Where it fits

Release **v1** (`EPIC-06` in frontmatter — not duplicated here). Depends on US-0021 — read those US files for what must already work. Stops before: Boundaries defined in `docs/epics/EPIC-06.md` and parent version `v1`.

### Approach

- Delivered scope recorded under `## Technical implementation`.
- Card: ID, title, epic name (not just code), MoSCoW.
- `done_when` and dependencies in `<details>` per card.
- Deliver acceptance item: Epic filters with abbreviated title.

### Architecture refs

- docs/05_architecture.md — CLI and script invocation from app
- docs/08_environments.md — Local development commands
- docs/epics/EPIC-06.md — Epic scope and out-of-scope

### API / DB impact

- _n/a_ — no API or database impact expected.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

## Technical implementation

### Frontend

- `KanbanView.tsx` — simplified `StoryCard`.

## Tests

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

### Planned

- [x] **manual** — filter EPIC-06; open details of a US.

### Executed

- Manual: filter EPIC-06; open details of a US.

## Out of scope for this story

- Boundaries defined in `docs/epics/EPIC-06.md` and parent version `v1`.

## Notes

- Completed story — delivery record in `## Technical implementation`. Version: `v1`.
