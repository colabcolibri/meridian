---
id: US-0030
title: Monitor shell with logo and refined background
epic: EPIC-06
version: v1
status: ✅
moscow: Should
depends_on:
done_when: Top bar uses favicon; page with subtle gradient; welcome screen aligned to tokens.
tests: required
tests_status: done
ready: true
---

# US-0030 — Monitor shell with logo and refined background

**As** Process Manager,
**I want** a more polished first impression when opening the app,
**so that** I trust the tool before opening the docs folder.

## Intent

### Acceptance

- [x] Top bar shows logo (`/favicon.svg`) instead of generic icon.
- [x] Page background with subtle gradient (`from-meridian-muted`).
- [x] Welcome screen uses `monitorPanelClass` and `typeScale`.
- [x] Evidence: `MonitorTopBar.tsx`, `monitor-layout.ts`, `WelcomeScreen.tsx`.

### Why

This story delivered **Monitor shell with logo and refined background**: Top bar shows logo (`/favicon.svg`) instead of generic icon.. It existed so that I trust the tool before opening the docs folder. The record of what shipped lives under `## Technical implementation`.

### Where

Release **v1** (`EPIC-06` in frontmatter — not duplicated here). Depends on US-0019 — read those US files for what must already work. Stops before: Boundaries defined in `docs/epics/EPIC-06.md` and parent version `v1`.

## Plan

### Approach

- Primary files: `src/features/monitor/components/MonitorTopBar.tsx`, `src/features/monitor/monitor-layout.ts`, `src/features/monitor/components/WelcomeScreen.tsx`.
- Top bar shows logo (`/favicon.svg`) instead of generic icon.
- Page background with subtle gradient (`from-meridian-muted`).
- Deliver acceptance item: Welcome screen uses `monitorPanelClass` and `typeScale`.

### Architecture refs

- docs/05_architecture.md — CLI and script invocation from app
- docs/08_environments.md — Local development commands
- docs/epics/EPIC-06.md — Epic scope and out-of-scope

### API / DB impact

- _n/a_ — no API or database impact expected.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Planned

- [x] **manual** — open app without folder — welcome readable on mobile and desktop.

## Record

### Files

- `src/features/monitor/components/MonitorTopBar.tsx` — favicon logo in top bar
- `src/features/monitor/monitor-layout.ts` — `MONITOR_CONTAINER`, page gradient background
- `src/features/monitor/components/WelcomeScreen.tsx` — `monitorPanelClass` and `typeScale`

### Backend

- _n/a_

### Frontend

- Top bar shows `/favicon.svg` instead of a generic icon.
- Page background uses subtle gradient (`from-meridian-muted`).
- Welcome screen aligned to monitor design tokens for mobile and desktop.

### Scripts / Docs

- _n/a_

### Executed

- Manual: open app without folder — welcome readable on mobile and desktop.

## Boundaries

### Out of scope for this story

- Boundaries defined in `docs/epics/EPIC-06.md` and parent version `v1`.

### Notes

- Completed story — delivery record in `## Technical implementation`. Version: `v1`.
