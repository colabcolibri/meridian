---
id: US-0050
title: Epic and version file templates
epic: EPIC-05
version: v2
status: ❌
moscow: Should
depends_on:
done_when: Commands create or open epic and version files from kit templates under docs/epics/ and docs/versions/.
tests: required
tests_status: pending
ready: true
---

# US-0050 — Epic and version file templates

**As** Future VSCode User,
**I want** templates for epic and version files,
**so that** I can extend delivery docs on disk without leaving the editor.

## Intent

### Acceptance

- [ ] Command **Meridian: New Epic** — picks next EPIC-XX id, writes `docs/epics/EPIC-XX.md` from `create-epic` template.
- [ ] Command **Meridian: New Version** — prompts for vX id, writes `docs/versions/vX.md` from `create-version` template.
- [ ] Validates no duplicate id before write.
- [ ] Opens file in editor; status `draft` or `planned` per template defaults.
- [ ] Does not auto-create US or sprints.

### Why

This story adds **Epic and version file templates** — templates for epic and version files. When done, I can extend delivery docs on disk without leaving the editor. It is intentionally one slice; it does not deliver the whole epic alone.

### Where

Release **v2** (`EPIC-05` in frontmatter — not duplicated here). Depends on US-0049 — read those US files for what must already work. Stops before: Sprint file creation (`/plan-sprint` stays agent workflow for v2).

## Plan

### Approach

- Scope: Epic and version file templates under `v2`.
- Command **Meridian: New Epic** — picks next EPIC-XX id, writes `docs/epics/EPIC-XX.md` from `create-epic` template.
- Command **Meridian: New Version** — prompts for vX id, writes `docs/versions/vX.md` from `create-version` template.
- Deliver acceptance item: Validates no duplicate id before write.

### Architecture refs

- docs/05_architecture.md — Editor integration and disk writes
- docs/02_security.md — Local file access and agent boundaries
- docs/epics/EPIC-05.md — Epic scope and out-of-scope

### API / DB impact

- Documentation-only — no API or database changes.

### Security notes

- Follow `docs/02_security.md` — local file access, secrets, and agent write boundaries.

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Planned

- [ ] **manual** — New Epic → valid frontmatter; `validate_meridian.py` passes.
- [ ] **manual** — Duplicate epic id → blocked.

## Record

### Files

_(fill in when implementation is complete)_

### Backend

_(fill in when applicable)_

### Frontend

_(fill in when applicable)_

### Scripts / Docs

_(fill in when applicable)_

### Executed

_(pending)_

## Boundaries

### Out of scope for this story

- Sprint file creation (`/plan-sprint` stays agent workflow for v2).
- Editing epics/versions via web monitor.

### Notes

- Satisfies v2 "writes to docs/epics/ and docs/versions/" at template level; full wizards deferred.
