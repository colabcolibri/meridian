---
id: US-0019
title: Consistent monitor visual shell
epic: EPIC-06
version: v1
status: ✅
moscow: Must
depends_on:
done_when: Layout uses single max width, spacing, and background consistent across all views.
tests: required
tests_status: done
ready: true
---

# US-0019 — Consistent monitor visual shell

**As** Process Manager,
**I want** a screen with predictable visual rhythm,
**so that** I don't get lost between loose blocks.

## Intent

### Acceptance

- [x] Shared layout class (`monitor-layout.ts`).
- [x] Main content in single column with `max-w-6xl`.
- [x] Background and cards aligned to existing shadcn theme.
- [x] Evidence: `monitor-layout.ts`, unified dashboard.

### Why

This story delivered **Consistent monitor visual shell**: Shared layout class (`monitor-layout.ts`).. It existed so that I don't get lost between loose blocks. The record of what shipped lives under `## Technical implementation`.

### Where

Release **v1** (`EPIC-06` in frontmatter — not duplicated here). Depends on US-0018 — read those US files for what must already work. Stops before: Dark theme.

## Plan

### Approach

- Delivered scope recorded under `## Technical implementation`.
- Shared layout class (`monitor-layout.ts`).
- Main content in single column with `max-w-6xl`.
- Deliver acceptance item: Background and cards aligned to existing shadcn theme.

### Architecture refs

- docs/05_architecture.md — CLI and script invocation from app
- docs/08_environments.md — Local development commands
- docs/epics/EPIC-06.md — Epic scope and out-of-scope

### API / DB impact

- _n/a_ — no API or database impact expected.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Planned

- [x] **manual** — switch tabs and verify horizontal alignment.

## Record

### Files

_(fill on close)_

### Backend

_(fill on close or \_n/a_)\_

### Frontend

- `monitor-layout.ts`, `MonitorDashboard` reorganized.

### Scripts / Docs

_(fill on close or \_n/a_)\_

### Executed

- Manual: switch tabs and verify horizontal alignment.

## Boundaries

### Out of scope for this story

- Dark theme.

### Notes

- Completed story — delivery record in `## Technical implementation`. Version: `v1`.
