---
id: US-0006
title: Organize .agent operational layer
epic: EPIC-05
version: v0
status: ✅
moscow: Must
depends_on:
  - US-0005
done_when: .agent contains agents, skills, workflows, rules, and scripts in Meridian standard.
tests: required
tests_status: done
---

# US-0006 — Organize .agent operational layer

**As** Process Manager,
**I want** the agent operational layer to use a formal `.agent` structure,
**so that** agents, skills, workflows, rules, and scripts are organized and reusable.

## Acceptance

- `.agent/ARCHITECTURE.md` documents the architecture of the agent layer.
- `.agent/agents/` contains initial Meridian agents.
- `.agent/skills/` contains skills as packages with `SKILL.md`.
- `.agent/workflows/` contains operational workflows.
- `.agent/rules/` contains global rules.
- `.agent/scripts/` contains the initial validation script.
- `meridian.md` documents `.agent/` as the official structure.

## Context & constraints

### Architecture refs

- docs/05_architecture.md — Editor integration and disk writes
- docs/02_security.md — Local file access and agent boundaries
- docs/epics/EPIC-05.md — Epic scope and out-of-scope

### API / DB impact

- _n/a_ — no API or database impact expected.

### Security notes

- Follow `docs/02_security.md` — local file access, secrets, and agent write boundaries.

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Implementation hints (preliminary)

- Delivered scope recorded under `## Technical implementation`.
- `.agent/ARCHITECTURE.md` documents the architecture of the agent layer.
- `.agent/agents/` contains initial Meridian agents.

## Technical implementation

### Frontend

- No mandatory functional change.
- Home continues mentioning skills as part of the kit for agents.

## Tests

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

### Planned

- [x] **lint** — `pnpm format:check`
- [x] **lint** — `pnpm lint`
- [x] **build** — `pnpm build`
- [x] **validate** — `python3 .agent/scripts/validate_meridian.py app-desktop`

### Executed

- `pnpm format:check`, `pnpm lint`, and `pnpm build` passed.
- `python3 .agent/scripts/validate_meridian.py app-desktop` passed.

## Out of scope for this story

- Create a CLI installer for the `.agent` layer.
- Create a UI to manage agents/workflows.

## Notes

- The structural reference came from the pattern observed in `antigravity-kit`, adapted for Meridian.
