---
id: US-0016
title: Remove mock data.ts as source
epic: EPIC-02
version: v1
status: ✅
moscow: Should
depends_on:
  - US-0011
  - US-0012
  - US-0013
done_when: No monitor tab imports phaseDocuments/userStories/epics from data.ts; only folder data or empty state.
tests: required
tests_status: done
---

# US-0016 — Remove mock data.ts as source

**As** Process Manager,
**I want** the app to only show data from the opened folder,
**so that** I manage with the same usage as agents (no mock drift).

## Acceptance

- [x] `data.ts` removed.
- [x] No folder open: empty state guiding **Open folder** (no fake data).
- [x] Three tabs work with `app-desktop/docs/` after opening the docs folder.
- [x] `pnpm build` and tests pass.

## Technical implementation

### Frontend

- `ProjectDataProvider` as the sole source after opening folder.
- Removed `src/domain/meridian/data.ts`.

## Tests

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

### Planned

- [x] **grep** — Grep: no import of `meridian/data` in `src/`.
- [x] **automated** — `pnpm test` and `pnpm build` passed.

### Executed

- Grep: no import of `meridian/data` in `src/`.
- `pnpm test` and `pnpm build` passed.

## Out of scope for this story

- Optional demo seed (Could in a future version).

## Notes

- Closes dogfooding for v1-S1.
