---
id: US-0016
title: Remove mock data.ts as source
epic: EPIC-02
version: v1
status: ✅
moscow: Should
depends_on:
done_when: No monitor tab imports phaseDocuments/userStories/epics from data.ts; only folder data or empty state.
tests: required
tests_status: done
ready: true
---

# US-0016 — Remove mock data.ts as source

**As** Process Manager,
**I want** the app to only show data from the opened folder,
**so that** I manage with the same usage as agents (no mock drift).

## Intent

### Acceptance

- [x] `data.ts` removed.
- [x] No folder open: empty state guiding **Open folder** (no fake data).
- [x] Three tabs work with `app-desktop/docs/` after opening the docs folder.
- [x] `pnpm build` and tests pass.

### Why

This story delivered **Remove mock data.ts as source**: `data.ts` removed.. It existed so that I manage with the same usage as agents (no mock drift). The record of what shipped lives under `## Technical implementation`.

### Where

Release **v1** (`EPIC-02` in frontmatter — not duplicated here). Depends on US-0011 and US-0012 and US-0013 — read those US files for what must already work. Stops before: Optional demo seed (Could in a future version).

## Plan

### Approach

- Primary files: `src/domain/meridian/data.ts`.
- `data.ts` removed.
- No folder open: empty state guiding **Open folder** (no fake data).
- Deliver acceptance item: Three tabs work with `app-desktop/docs/` after opening the docs folder.

### Architecture refs

- docs/05_architecture.md — Project folder access and docs/ layout
- docs/04_principles.md — Docs-first and local-first operation
- docs/epics/EPIC-02.md — Epic scope and out-of-scope

### API / DB impact

- Documentation-only — no API or database changes.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Planned

- [x] **grep** — Grep: no import of `meridian/data` in `src/`.
- [x] **automated** — `pnpm test` and `pnpm build` passed.

## Record

### Files

- `app-desktop/src/features/folder/ProjectDataContext.tsx` — sole data source after open
- `app-desktop/src/domain/meridian/data.ts` — removed (mock no longer imported)

### Backend

_n/a_

### Frontend

- `ProjectDataProvider` as the sole source after opening folder.
- Removed `src/domain/meridian/data.ts`.

### Scripts / Docs

_n/a_

### Executed

- Grep: no import of `meridian/data` in `src/`.
- `pnpm test` and `pnpm build` passed.

## Boundaries

### Out of scope for this story

- Optional demo seed (Could in a future version).

### Notes

- Closes dogfooding for v1-S1.
