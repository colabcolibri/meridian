---
id: US-0016
title: Remover mock data.ts como fonte
epic: EPIC-02
version: v1
status: ✅
moscow: Should
depends_on:
  - US-0011
  - US-0012
  - US-0013
done_when: Nenhuma aba do monitor importa phaseDocuments/userStories/epics de
  data.ts; só dados da pasta ou estado vazio.
tests: required
tests_status: done
---

# US-0016 — Remover mock data.ts como fonte

**Como** Manager do Processo,
**quero** que o app só mostre dados da pasta aberta,
**para que** eu gerencie pelo mesmo uso que os agents (sem drift de mock).

## Aceite

- [x] `data.ts` removido.
- [x] Sem pasta aberta: empty state orientando **Abrir pasta** (sem dados fake).
- [x] Três abas funcionam com `app-desktop/docs/` após abrir pasta docs.
- [x] `pnpm build` e testes passam.

## Implementação técnica

### Frontend

- `ProjectDataProvider` como única fonte após abrir pasta.
- Removido `src/domain/meridian/data.ts`.

## Testes

> Na **criação**: preencher **Planejado**. Ao **fechar** (`complete-user-story`): marcar `[x]` e registrar em **Executado**.

> Na **criação**: preencher **Planejado**. Ao **fechar** (`complete-user-story`): marcar `[x]` e registrar em **Executado**.

### Planejado

- [x] **grep** — Grep: nenhum import de `meridian/data` em `src/`.
- [x] **automated** — `pnpm test` e `pnpm build` passaram.

### Executado

- Grep: nenhum import de `meridian/data` em `src/`.
- `pnpm test` e `pnpm build` passaram.

## Fora de escopo desta story

- Seed de demo opcional (Could em versão futura).

## Notas

- Fecha dogfooding da v1-S1.
