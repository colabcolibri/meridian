---
id: US-0042
title: Detect Meridian workspace in extension
epic: EPIC-05
version: v2
status: ❌
moscow: Must
depends_on:
  - US-0041
done_when: Extension detects .agent/MERIDIAN.md and docs/ folder and exposes status in the status bar.
tests: required
tests_status: pending
---

# US-0042 — Detect Meridian workspace in extension

**As** Future VSCode User,
**I want** the extension to recognize a Meridian project in my workspace,
**so that** commands only run when the folder structure is valid.

## Acceptance

- [ ] On activation, extension checks for `.agent/MERIDIAN.md` at workspace root (or nested project root per architecture).
- [ ] Resolves `docs/` path relative to workspace; warns if missing.
- [ ] Status bar item shows "Meridian" with tooltip: kit detected, docs path, US count (read-only).
- [ ] Commands disabled or show clear message when workspace is not Meridian.
- [ ] Works when workspace root is `meridian/` monorepo or a client project with only `.agent/` + `docs/`.

## Technical implementation

> On **creation**: placeholder below. On **completion** (`✅`): skill `complete-user-story` — replace with real record (files + layers).

### Files

_(fill in when implementation is complete)_

### Backend

_(fill in when applicable)_

### Frontend

_(fill in when applicable)_

### Scripts / Docs

_(fill in when applicable)_

## Tests

### Planned

- [ ] **manual** — Open `meridian/` repo → status bar shows Meridian active.
- [ ] **manual** — Open empty folder → status bar shows not a Meridian project; commands blocked.

### Executed

_(pending)_

## Out of scope for this story

- File writes.
- Multi-root workspace edge cases beyond first folder with `.agent/`.

## Notes

- Align detection rules with `validate_meridian.py` kit detection (`.agent/MERIDIAN.md`).
