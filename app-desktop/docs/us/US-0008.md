---
id: US-0008
title: Monitor with three product tabs
epic: EPIC-02
version: v0
status: ✅
moscow: Must
depends_on:
  - US-0002
done_when: App shows Initial Setup, Epics, and Kanban tabs with readable state per doc/US.
tests: required
tests_status: done
---

# US-0008 — Monitor with three product tabs

**As** Process Manager,
**I want** to switch between initial setup, epics, and kanban in the same app,
**so that** I manage the Meridian project in the flow that the protocol defines.

## Acceptance

- [x] Three visible tabs: Initial setup, Epics, Kanban.
- [x] Initial setup groups phase docs (00–08 and 11) by phase with a label per step.
- [x] Epics list EPIC-01…05 with description, versions, and US count.
- [x] Kanban groups US by status (`❌` `🔶` `✅` `🧊`) with epic filter.
- [x] No double column "status + released" on the same line.
- [x] `pnpm build` passes.

## Context & constraints

### Why this story

This story delivered **Monitor with three product tabs**: Three visible tabs: Initial setup, Epics, Kanban.. It existed so that I manage the Meridian project in the flow that the protocol defines. The record of what shipped lives under `## Technical implementation`.

### Where it fits

Release **v0** (`EPIC-02` in frontmatter — not duplicated here). Depends on US-0002 — read those US files for what must already work. Stops before: Open real folder and Markdown parser (v1 — US-0009+).

### Approach

- Primary files: `src/features/monitor/`, `src/domain/meridian/`.
- Three visible tabs: Initial setup, Epics, Kanban.
- Initial setup groups phase docs (00–08 and 11) by phase with a label per step.
- Deliver acceptance item: Epics list EPIC-01…05 with description, versions, and US count.

### Architecture refs

- docs/05_architecture.md — Project folder access and docs/ layout
- docs/04_principles.md — Docs-first and local-first operation
- docs/epics/EPIC-02.md — Epic scope and out-of-scope

### API / DB impact

- _n/a_ — no API or database impact expected.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

## Technical implementation

### Frontend

- `src/features/monitor/` with `MonitorTabs`, `SetupMonitorView`, `EpicsView`, `KanbanView`.
- Domain in `src/domain/meridian/` (validators, types, mock in `data.ts` until v1).

## Tests

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

> On **creation**: fill in **Planned**. On **close** (`complete-user-story`): mark `[x]` and record in **Executed**.

### Planned

- [x] **build** — `pnpm build` passed after reorganizing the three views.

### Executed

- `pnpm build` passed after reorganizing the three views.

## Out of scope for this story

- Open real folder and Markdown parser (v1 — US-0009+).
- Edit docs or US through the app.

## Notes

- Closes v0-S2. Data still mock; real reading unblocks US-0002 for ✅.
