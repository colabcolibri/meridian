---
id: US-0052
title: Extension kit docs and CI
epic: EPIC-05
version: v2
status: ❌
moscow: Should
depends_on:
  - US-0046
done_when: Kit documents extension workflow and GitHub Actions runs extension compile/lint on PR.
tests: required
tests_status: pending
ready: true
---

# US-0052 — Extension kit docs and CI

**As** Process Manager,
**I want** documented extension workflow and CI,
**so that** contributors and agents know how to build and verify v2.

## Acceptance

- [ ] `.agent/references/usage-guide.md` — section "VS Code extension" with install, F5, Sync Board, New US flow.
- [ ] Root `README.md` — v2 status `active`, link to `app-visual-studio/README.md`.
- [ ] GitHub Actions job: `pnpm --filter app-visual-studio compile` (or equivalent) on PR touching `app-visual-studio/`.
- [ ] CI runs `generate_board.py` smoke test on dogfood docs (optional job step).
- [ ] No secrets required in CI.

## Context & constraints

### Why this story

This story adds **Extension kit docs and CI** — documented extension workflow and CI. When done, contributors and agents know how to build and verify v2. It is intentionally one slice; it does not deliver the whole epic alone.

### Where it fits

Release **v2** (`EPIC-05` in frontmatter — not duplicated here). Depends on US-0046 — read those US files for what must already work. Stops before: Marketplace publish pipeline.

### Approach

- Scope: Extension kit docs and CI under `v2`.
- `.agent/references/usage-guide.md` — section "VS Code extension" with install, F5, Sync Board, New US flow.
- Root `README.md` — v2 status `active`, link to `app-visual-studio/README.md`.
- Deliver acceptance item: GitHub Actions job: `pnpm --filter app-visual-studio compile` (or equivalent) on PR touching `app-visual-studio/`.

### Architecture refs

- docs/05_architecture.md — Editor integration and disk writes
- docs/02_security.md — Local file access and agent boundaries
- docs/epics/EPIC-05.md — Epic scope and out-of-scope

### API / DB impact

- _n/a_ — no API or database impact expected.

### Security notes

- Follow `docs/02_security.md` — local file access, secrets, and agent write boundaries.

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

## Technical implementation

> On **creation**: placeholder below. On **completion** (`✅`): skill `complete-user-story` — replace with real record (files + layers).

### Files

_(fill in when implementation is complete)_

### Backend

_(fill in when applicable)_

### Frontend

_(fill in when applicable)_

### Scripts / Docs

_(fill in when applicable)_

## Tests

### Planned

- [ ] **manual** — Follow usage-guide steps on clean clone → extension runs.
- [ ] **automated** — CI workflow green on sample PR.

### Executed

_(pending)_

## Out of scope for this story

- Marketplace publish pipeline.
- Cursor-specific extension packaging.

## Notes

- Monorepo may need pnpm workspace entry for `app-visual-studio`.
