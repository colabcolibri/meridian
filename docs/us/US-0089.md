---
id: US-0089
title: Board kanban webview in VS Code extension sidebar
epic: EPIC-05
version: v4
status: ✅
moscow: Must
depends_on: []
ready: true
done_when: "Installable extension shows Meridian activity bar with Board webview — kanban columns from docs/us/, card click opens US file."
tests: required
tests_status: done
---

# US-0089 — Board kanban webview in VS Code extension sidebar

**As** Process Manager,
**I want** a kanban board in the VS Code/Cursor sidebar when my workspace uses Meridian,
**so that** I can track user stories without opening the browser monitor.

## Intent

### Acceptance

- [x] Activity bar container **Meridian** with **Board** webview (`meridian.board`).
- [x] Columns follow monitor rules (`kanban-columns`: ❌ 🔶 🧪 ✅ 🧊).
- [x] Cards show id, title, epic, version, moscow from `docs/us/*.md` frontmatter.
- [x] Click card opens `docs/us/US-XXXX.md` in the editor.
- [x] Refreshes when US files change; works on monorepo `app-desktop/docs/`.
- [x] `pnpm compile` + `pnpm test` pass in `app-visual-studio/`.

### Why

v4 repivot: the extension is an **installable** visual monitor in the IDE, not a sync command shell. Agents already maintain files; the extension **displays** the board.

### Where

v4-S2 (replanned). Builds on US-0041/0042. Deliverables webview → next US.

## Plan

### Approach

- `WebviewViewProvider` + HTML/CSS kanban (VS Code theme variables).
- Extension-local parser + `groupStoriesForKanban` aligned with `app-desktop` domain.
- `yaml` dependency for frontmatter.

### Architecture refs

- `docs/05_architecture.md` — VS Code extension (v4)
- `app-desktop/src/domain/meridian/kanban-columns.ts` — column rules

### API / DB impact

_n/a_

### Security notes

Read-only on `docs/`; webview CSP restricts scripts; card click opens local files only.

### Related decisions

- `docs/decisions/2026-06-04.json` — v4 repivot (kanban in IDE).

### Planned

- [x] **automated** — `pnpm test` in `app-visual-studio/`
- [x] **manual** — F5 → Meridian icon → Board columns → click US opens file

## Record

### Files

- `app-visual-studio/package.json` — viewsContainers, views, v0.1.0
- `app-visual-studio/media/meridian.svg`
- `app-visual-studio/src/board-panel.ts`
- `app-visual-studio/src/load-stories.ts`
- `app-visual-studio/src/domain/{types,parse-us,kanban}.ts`
- `app-visual-studio/src/extension.ts`, `meridian-context.ts`
- `app-visual-studio/test/kanban.test.ts`

### Backend

_n/a_

### Frontend

_n/a_ — webview HTML/CSS in `board-panel.ts` (extension host UI).

### Scripts / Docs

- `app-visual-studio/README.md` — Board usage
- `app-desktop/docs/sprints/v4-S2.md`, `docs/versions/v4.md`

### Executed

- automated — `pnpm install && pnpm test` in `app-visual-studio/` — passed
- **suggested commit:** `feat(vscode): Board kanban sidebar webview from docs/us (US-0089)`

## Boundaries

### Out of scope for this story

- Deliverables (epics/versions) webview — next US.
- Drag-and-drop status change.
- Marketplace publish (later).

### Notes

- Column logic mirrors `kanban-columns.ts`; parser is extension-local for installable bundle size.
