---
id: US-0093
title: v4 go-live — extension package, architecture sync, release closure
epic: EPIC-05
version: v4
status: ✅
moscow: Must
depends_on: [US-0092]
ready: true
done_when: "v4 marked complete; 05_architecture and v4 go-live checklist done; .vsix builds; EPIC-05 closed for v4 scope."
tests: required
tests_status: done
---

# US-0093 — v4 go-live — extension package, architecture sync, release closure

**As** Process Manager,
**I want** version v4 and EPIC-05 closed with accurate architecture docs and a installable `.vsix`,
**so that** the team can dogfood the extension without treating v4 as still in progress.

## Intent

### Acceptance

- [x] `docs/05_architecture.md` describes shipped UX: Commands sidebar, Board/Deliverables **editor tabs**, filters (All/None/multi-select), `Meridian: Validate Project` (Python), sync/new-US stubs.
- [x] `docs/versions/v4.md` go-live checklist complete; version `status: complete`.
- [x] `docs/epics/EPIC-05.md` marked `complete` for v4 deliverables (v5 writes remain deferred).
- [x] Sprint `v4-S4` complete with this US.
- [x] `pnpm package:vsix` succeeds in `app-visual-studio/`.
- [x] `validate_meridian.py app-desktop` passes.

### Why

US-0089 through US-0092 delivered the product slice; without release closure the repo still showed v4 `planned` and architecture text implied sidebar kanban and extension writes. Managers need a single truth before starting v5 (sync/new US from the extension).

### Where

v4-S4. Depends on US-0092 (filter/accordion parity). Closes EPIC-05 for the IDE read surface; v5 picks up disk writes and board generation from the extension.

## Plan

### Approach

- Sync `05_architecture.md`, `v4.md`, `EPIC-05.md`, `v4-S4.md`.
- Run `pnpm package:vsix` and document install path in `app-visual-studio/README.md`.
- Prepend decision entry for v4 go-live.

### Architecture refs

- `docs/05_architecture.md` — § VS Code extension (v4)

### API / DB impact

_n/a_

### Security notes

_n/a_

### Related decisions

- `docs/decisions/2026-06-04.json` — v4 go-live entry

### Planned

- [x] **automated** — `pnpm package:vsix` + `pnpm test` + `validate_meridian.py app-desktop`
- [x] **manual** — `pnpm install:cursor` → Open Board + Deliverables on `app-desktop/docs/`

## Record

### Files

- `app-desktop/docs/05_architecture.md`
- `app-desktop/docs/versions/v4.md`
- `app-desktop/docs/epics/EPIC-05.md`
- `app-desktop/docs/sprints/v4-S4.md`
- `app-desktop/docs/decisions/2026-06-04.json`
- `app-desktop/docs/us/US-0093.md`
- `app-desktop/docs/kanban/board.json`
- `app-visual-studio/README.md` — go-live smoke steps

### Backend

_n/a_

### Frontend

_n/a_

### Scripts / Docs

- Release closure only; extension code unchanged in this US unless vsix script fix required.

### Executed

- automated — `pnpm package:vsix` + `pnpm test` + `validate_meridian.py app-desktop`
- **suggested commit:** `chore(v4): go-live docs, architecture sync, EPIC-05 complete (US-0093)`

## Boundaries

### Out of scope for this story

- Marketplace publish (publisher id, listing).
- v5 sync/new US implementation.

### Notes

- Manual smoke on Cursor/VS Code remains the manager habit after `install:cursor`; documented in README.
