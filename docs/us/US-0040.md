---
id: US-0040
title: Decisions tab in monitor
epic: EPIC-06
version: v1
status: ✅
moscow: Must
depends_on:
done_when: Decisions tab lists days and entries from docs/decisions/ folder with structured detail in side sheet.
tests: required
tests_status: done
ready: true
---

# US-0040 — Decisions tab in monitor

**As** Process Manager,
**I want** a dedicated tab for decision history,
**so that** I can consult the log without opening JSON in the editor.

## Intent

### Acceptance

- [x] New **Decisions** tab between Setup and Deliverables (`MonitorTabs.tsx`).
- [x] List grouped by date (most recent first); expandable days.
- [x] Each entry shows time, title, affected document, and summary of `what_changed`.
- [x] Click opens sheet with all structured fields (without depending on markdown).
- [x] Empty state guides creating `YYYY-MM-DD.json` in `docs/decisions/`.
- [x] **Get Started** guide mentions the `decisions/` folder and JSON prepend.

### Why

This story delivered **Decisions tab in monitor**: New **Decisions** tab between Setup and Deliverables (`MonitorTabs.tsx`).. It existed so that I can consult the log without opening JSON in the editor. The record of what shipped lives under `## Technical implementation`.

### Where

Release **v1** (`EPIC-06` in frontmatter — not duplicated here). Depends on US-0039 — read those US files for what must already work. Stops before: Boundaries defined in `docs/epics/EPIC-06.md` and parent version `v1`.

## Plan

### Approach

- Delivered scope recorded under `## Technical implementation`.
- New **Decisions** tab between Setup and Deliverables (`MonitorTabs.tsx`).
- List grouped by date (most recent first); expandable days.
- Deliver acceptance item: Each entry shows time, title, affected document, and summary of `what_changed`.

### Architecture refs

- docs/05_architecture.md — CLI and script invocation from app
- docs/08_environments.md — Local development commands
- docs/epics/EPIC-06.md — Epic scope and out-of-scope

### API / DB impact

- Documentation-only — no API or database changes.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Planned

- [x] **manual** — open `app-desktop/docs/` → Decisions tab → see 2026-06-02 with entries.
- [x] **manual** — click entry → sheet with all fields.

## Record

### Files

- `app-desktop/src/features/monitor/components/DecisionsView.tsx` — Decisions tab UI
- `app-desktop/src/features/monitor/MonitorDashboard.tsx` — Decisions tab route
- `app-desktop/src/features/monitor/components/MonitorTabs.tsx` — tab entry
- `app-desktop/src/features/folder/ProjectDataContext.tsx` — `decisionDays` on project data
- `app-desktop/src/domain/meridian/types.ts` — `DecisionEntry`, `DecisionDay`
- `app-desktop/src/domain/meridian/meridian-concepts.ts` — decisions folder guide

### Backend

_n/a_

### Frontend

| Layer | Files                                                          |
| ----- | -------------------------------------------------------------- |
| UI    | `DecisionsView.tsx`, `MonitorDashboard.tsx`, `MonitorTabs.tsx` |
| Data  | `MeridianProjectData.decisionDays`, `ProjectDataContext.tsx`   |
| Types | `types.ts` — `DecisionEntry`, `DecisionDay`                    |
| Guide | `meridian-concepts.ts` — decisions folder + prepend flow       |

### Scripts / Docs

_n/a_

### Executed

- Manual: Decisions tab lists 31 entries in 2026-06-02; sheet displays JSON fields.

## Boundaries

### Out of scope for this story

- Boundaries defined in `docs/epics/EPIC-06.md` and parent version `v1`.

### Notes

- Completed story — delivery record in `## Technical implementation`. Version: `v1`.
