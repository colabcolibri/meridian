---
id: US-0024
title: Kanban with version badge and filter
epic: EPIC-04
version: v1
status: ✅
moscow: Must
depends_on:
done_when: Each card shows version badge; board filters by v0/v1/v2.
tests: required
tests_status: done
ready: true
---

# US-0024 — Kanban with version badge and filter

**As** Process Manager,
**I want** to filter the board by product version,
**so that** I see only the US for the current release.

## Intent

### Acceptance

- [x] `vX` badge visible at the top of each card (not only in `<details>`).
- [x] Version filter above columns (All + each `version` from US).
- [x] Version filter combines with epic filter.
- [x] Evidence: `KanbanView.tsx` — `versionFilter` state and badge in `StoryCard`.

### Why

This story delivered **Kanban with version badge and filter**: `vX` badge visible at the top of each card (not only in `<details>`).. It existed so that I see only the US for the current release. The record of what shipped lives under `## Technical implementation`.

### Where

Release **v1** (`EPIC-04` in frontmatter — not duplicated here). Depends on US-0022 — read those US files for what must already work. Stops before: Boundaries defined in `docs/epics/EPIC-04.md` and parent version `v1`.

## Plan

### Approach

- Delivered scope recorded under `## Technical implementation`.
- `vX` badge visible at the top of each card (not only in `<details>`).
- Version filter above columns (All + each `version` from US).
- Deliver acceptance item: Version filter combines with epic filter.

### Architecture refs

- docs/05_architecture.md — Monitor, kanban and project loader
- docs/epics/EPIC-04.md — Kanban and user story boundaries
- docs/epics/EPIC-04.md — Epic scope and out-of-scope

### API / DB impact

- _n/a_ — no API or database impact expected.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Planned

- [x] **manual** — filter `v1`; confirm that v0 US disappear from columns.

## Record

### Files

- `app-desktop/src/features/monitor/components/KanbanView.tsx` — version filter state and badges

### Backend

_n/a_

### Frontend

- `KanbanView.tsx` — `versionFilter` state and badges.

### Scripts / Docs

_n/a_

### Executed

- Manual: filter `v1`; confirm that v0 US disappear from columns.

## Boundaries

### Out of scope for this story

- Boundaries defined in `docs/epics/EPIC-04.md` and parent version `v1`.

### Notes

- Completed story — delivery record in `## Technical implementation`. Version: `v1`.
