---
id: US-0046
title: Meridian Validate Project command
epic: EPIC-05
version: v4
status: ✅
moscow: Must
depends_on: [US-0042]
ready: true
done_when: Command Meridian Validate Project runs validate_meridian.py and lists errors and warnings in the editor.
tests: required
tests_status: done
---

# US-0046 — Meridian Validate Project command

**As** Future VSCode User,
**I want** to validate my Meridian folder from the editor,
**so that** I catch protocol issues before committing.

## Intent

### Acceptance

- [x] Command palette: **Meridian: Validate Project** (`meridian.validateProject`).
- [x] Runs `python3 .agent/scripts/validate_meridian.py <project-root>`.
- [x] Output channel lists errors and warnings with file paths (**Meridian Validate**).
- [x] Notification summary: pass on exit 0; error toast on failure.
- [x] Works on monorepo dogfood (`app-desktop/` → validates parent with `docs/`).

### Why

Managers need protocol checks from the IDE without running slash commands. Shipped with v4 extension (US-0042 workspace detection); complements **Sync Board** (US-0095).

### Where

Originally v2 scope; delivered in v4 extension. Problems panel integration remains out of scope.

## Plan

### Approach

- `validate-runner.ts` resolves target folder; spawns kit Python script.
- `extension.ts` wires command + **Meridian Validate** output channel.

### Architecture refs

- `docs/05_architecture.md` — § VS Code extension (v4)

### API / DB impact

_n/a_

### Security notes

Read-only validation; local Python on PATH.

### Related decisions

- `docs/decisions/2026-06-04.json` — US-0046 closed with v4 extension

### Planned

- [x] **automated** — `pnpm test` (extension compile + unit tests)
- [x] **manual** — Validate on `app-desktop/` → passed notification

## Record

### Files

- `app-visual-studio/src/validate-runner.ts`
- `app-visual-studio/src/extension.ts`
- `app-visual-studio/src/command-catalog.ts` (help reference)
- `app-desktop/docs/us/US-0046.md`

### Backend

_n/a_

### Frontend

_n/a_

### Scripts / Docs

_n/a_

### Executed

- automated — `pnpm test` — 26 passed
- **suggested commit:** `docs(us): close US-0046 validate command`

## Boundaries

### Out of scope for this story

- Problems panel diagnostics integration.
- Replacing dev-server validate bridge in browser monitor.

### Notes

- Output channel named **Meridian Validate** (not generic "Meridian") to separate from **Meridian Tools**.
