---
id: US-0034
title: Global version filter in monitor
epic: EPIC-04
version: v1
status: ✅
moscow: Must
depends_on:
done_when: Deliverables and Board share same version filter; default to active version or last with US.
tests: required
tests_status: done
ready: true
---

# US-0034 — Global version filter in monitor

**As** Process Manager,
**I want** to choose the version once and see Deliverables and Board aligned,
**so that** I don't repeat the filter or see out-of-focus releases.

## Intent

### Acceptance

- [x] Single version bar visible on Deliverables and Board tabs.
- [x] Switching tabs keeps the selected version.
- [x] Default: `active` version; otherwise last version with US in the folder.
- [x] Evidence: `MonitorVersionFilterContext.tsx`, `VersionFilterBar.tsx`, `MonitorDashboard.tsx`.

### Why

This story delivered **Global version filter in monitor**: Single version bar visible on Deliverables and Board tabs.. It existed so that I don't repeat the filter or see out-of-focus releases. The record of what shipped lives under `## Technical implementation`.

### Where

Release **v1** (`EPIC-04` in frontmatter — not duplicated here). Depends on US-0024 and US-0025 — read those US files for what must already work. Stops before: Boundaries defined in `docs/epics/EPIC-04.md` and parent version `v1`.

## Plan

### Approach

- Primary files: `src/features/monitor/MonitorVersionFilterContext.tsx`, `src/features/monitor/components/VersionFilterBar.tsx`, `src/features/monitor/MonitorDashboard.tsx`.
- Single version bar visible on Deliverables and Board tabs.
- Switching tabs keeps the selected version.
- Touch `src/features/monitor/version-filter.ts` as part of delivering this slice — keep changes aligned with acceptance.

### Architecture refs

- docs/05_architecture.md — Monitor, kanban and project loader
- docs/epics/EPIC-04.md — Kanban and user story boundaries
- docs/epics/EPIC-04.md — Epic scope and out-of-scope

### API / DB impact

- _n/a_ — no API or database impact expected.

### Security notes

- _n/a_

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Planned

- [x] **manual** — filter v1 on Board, go to Deliverables — same version focused.

## Record

### Files

- `src/features/monitor/MonitorVersionFilterContext.tsx` — shared version selection state
- `src/features/monitor/components/VersionFilterBar.tsx` — version filter UI
- `src/features/monitor/MonitorDashboard.tsx` — provider wiring across tabs
- `src/features/monitor/version-filter.ts` — default version resolution (`active` or latest with US)

### Backend

- _n/a_

### Frontend

- Single version bar on Deliverables and Board; selection persists when switching tabs.
- Default selects `active` version, otherwise the latest version that has user stories.

### Scripts / Docs

- _n/a_

### Executed

- Manual: filter v1 on Board, go to Deliverables — same version focused.

## Boundaries

### Out of scope for this story

- Boundaries defined in `docs/epics/EPIC-04.md` and parent version `v1`.

### Notes

- Completed story — delivery record in `## Technical implementation`. Version: `v1`.
