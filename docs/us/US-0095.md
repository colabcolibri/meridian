---
id: US-0095
title: Meridian Sync Board command writes board.json from docs/us
epic: EPIC-05
version: v4
status: ✅
moscow: Must
depends_on: [US-0042]
ready: true
done_when: "Meridian: Sync Board regenerates docs/kanban/board.json from US frontmatter and reports count in Output."
tests: required
tests_status: done
---

# US-0095 — Meridian Sync Board command writes board.json from docs/us

**As** Process Manager,
**I want** to regenerate `board.json` from the extension after editing user stories,
**so that** validate and external tools stay aligned without running chat agents.

## Intent

### Acceptance

- [x] **Meridian: Sync Board** writes `docs/kanban/board.json` from all parseable `docs/us/US-*.md` frontmatter.
- [x] JSON fields match board schema (`id`, `title`, `epic`, `version`, `status`, `moscow`, `depends_on`, `done_when`, `tests`, `tests_status`); sorted by id.
- [x] Success notification + **Meridian Tools** output with story count and path.
- [x] Board webview refresh after sync (unchanged read path from `docs/us/`).
- [x] Creates `docs/kanban/` if missing.
- [x] `pnpm test` includes `generate-board.test.ts`.

### Why

The command was a stub while the Board read US files directly — managers still need `board.json` for `validate_meridian.py`. A local TypeScript exporter avoids a missing `generate_board.py` and matches the generate-board-json skill contract.

### Where

v4-S5; closes deferred US-0045 scope for extension read/write slice. Board UI still reads `docs/us/` directly.

## Plan

### Approach

- `generate-board.ts` + `sync-board.ts`; wire `meridian.syncBoard` in `extension.ts`.

### Architecture refs

- `docs/05_architecture.md` — § VS Code extension (v4)
- `.agent/skills/generate-board-json/references/board-schema.md`

### API / DB impact

Writes derived `board.json` only.

### Security notes

Write limited to `docs/kanban/board.json` under resolved docs root.

### Related decisions

_n/a_

### Planned

- [x] **automated** — `pnpm test`
- [x] **manual** — Sync Board → validate; Board unchanged

## Record

### Files

- `app-visual-studio/src/generate-board.ts`, `sync-board.ts`
- `app-visual-studio/test/generate-board.test.ts`
- `app-visual-studio/src/extension.ts`, `commands-sidebar.ts`

### Backend

_n/a_

### Frontend

_n/a_

### Scripts / Docs

- `docs/05_architecture.md`

### Executed

- automated — `pnpm test` — 14 passed
- **suggested commit:** `feat(vscode): sync board.json from US frontmatter (US-0095)`

## Boundaries

### Out of scope for this story

- Auto-sync on every US save.
- Python `generate_board.py` in kit.

### Notes

- Board kanban display does not require sync; sync updates derived artifact only.
