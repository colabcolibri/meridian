---
id: US-0005
title: Create initial skills in .agent
epic: EPIC-05
version: v0
status: ✅
moscow: Must
depends_on:
done_when: .agent/skills contains initial skills for agents to use with .agent/MERIDIAN.md.
tests: required
tests_status: done
ready: true
---

# US-0005 — Create initial skills in .agent

**As** Process Manager,
**I want** to have auxiliary skills inside `.agent/skills`,
**so that** AI agents execute recurring Meridian tasks with more consistency.

## Intent

### Acceptance

- [ ] `.agent/skills/` folder created.
- [ ] Skills use packages with `SKILL.md` and frontmatter.
- [ ] Initial skills created for starting a project, reviewing security, creating US, generating board JSON, and updating decisions.
- [ ] `.agent/MERIDIAN.md` mentions `.agent/skills/` as the operational auxiliary layer.
- [ ] Security in `meridian.md` was deepened to cover secrets, agents, data, OWASP, dependencies, and logs.

### Why

This story delivered **Create initial skills in .agent**: `.agent/skills/` folder created.. It existed so that AI agents execute recurring Meridian tasks with more consistency. The record of what shipped lives under `## Technical implementation`.

### Where

Release **v0** (`EPIC-05` in frontmatter — not duplicated here). Depends on US-0004 — read those US files for what must already work. Stops before: Turn skills into an installable package.

## Plan

### Approach

- Delivered scope recorded under `## Technical implementation`.
- `.agent/skills/` folder created.
- Skills use packages with `SKILL.md` and frontmatter.
- Deliver acceptance item: Initial skills created for starting a project, reviewing security, creating US, generating board JSON, and updating decisions.

### Architecture refs

- docs/05_architecture.md — Editor integration and disk writes
- docs/02_security.md — Local file access and agent boundaries
- docs/epics/EPIC-05.md — Epic scope and out-of-scope

### API / DB impact

- Documentation-only — no API or database changes.

### Security notes

- Follow `docs/02_security.md` — local file access, secrets, and agent write boundaries.

### Related decisions

- _n/a_ — log in `docs/decisions/` if scope or acceptance changes.

### Planned

- [x] **lint** — `pnpm format:check`
- [x] **lint** — `pnpm lint`
- [x] **build** — `pnpm build`

## Record

### Files

- `app-desktop/src/App.tsx` — home copy aligned to kit + agents
- `.agent/skills/` — skills verified present for agent workflow

### Backend

_n/a_

### Frontend

- No functional change beyond keeping the home aligned to the concept.

### Scripts / Docs

_n/a_

### Executed

- `pnpm format:check`, `pnpm lint`, and `pnpm build` passed in `app-desktop/`.
- Initial skills verified in `.agent/skills/`.

## Boundaries

### Out of scope for this story

- Turn skills into an installable package.
- Create a skills management UI.

### Notes

- Skills are extra-app and serve AI agents directly.
