# Workflows removed (kit v3.1)

Meridian no longer uses `.agent/workflows/`. **Procedures live in skills only.**

| You want… | Type in chat |
| --------- | ------------ |
| Create US | `/us-create` or `@story-maker` |
| Database pass | `/data-engineering` or `@data-engineer` |
| Project status | `/project-status` or `@scrum-master` |

Full map: `.agent/references/guides/agents-help.md`

After clone: `./.agent/scripts/sync_kit.sh` — syncs `.agent/skills/` to IDE adapters (not commands).
